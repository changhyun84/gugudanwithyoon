# 구현 현황

`기술설계서.md`는 **설계 의도**를, 이 문서는 **실제로 구현된 것과 그 과정에서 바뀐 결정**을 기록합니다.
둘이 어긋나는 부분은 이 문서가 최신입니다.

마지막 갱신: 2026-08-19 — 캐릭터 넷 다시 그림(11장), 확장 6A~6D 계획(9장), 결정 변경 2건(10·12장)

---

## 1. 진행 상황

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 서버 뼈대, 프로필 CRUD, 원자적 저장 + 백업 | 완료 |
| 2 | v1 게임 로직 이식 (양 1종) | 완료 |
| 3 | CSV/MD 문제 팩, 오답 자동 생성, 자유 모드 | 완료 |
| 4 | 캐릭터 5종, 앵커 아이템, 별 해금, 응원 메시지 | 완료 |
| 5A | 계측(`facts.log`), 부모 현황·약한 문제, 문제 켜고 끄기 | 완료 |
| 5B | 보상 확장 — 도감·상점·배경·소원권·용돈 | **완료** |
| 5C | 학습 진도 관리 — 과목·단원 폴더, 진도 설정 (19~22장) | **완료** |
| 6A | 단답형 (`유형` 열, 입력 화면, 「보기 보여줘」) | **다음** |
| 6B | 사진으로 단어 등록 (`ai.py`, 부모 화면 전용) | 계획 |
| 6C | 서술형 — AI 없이 (쓰기 화면, `writings`, 이야기 판, **선생님 등장**) | 계획 |
| 6D | 서술형 AI 반응·조언 (`/api/ai/reply`, 선생님 페르소나) | 계획 |
| 7 | 모드 확장 — 다시 만나기, 스피드 | 미착수 |
| 8 | 마감 — PWA, 오프라인, 백업 UI, 접근성 | 미착수 |

실사용 중입니다. 집 PC(Windows)에서 `시작하기.bat`으로 띄우고 아이패드로 접속합니다.

6A~6D는 아이가 요청한 확장(단답형·서술형·영어 단어)입니다. 기획서 12~15장에 배경과 순서 근거가 있습니다. **원래 6단계였던 모드 확장은 7로 밀렸습니다** — 스피드 모드보다 이쪽이 실제 학습 범위를 넓힙니다.

---

## 2. 설계서와 달라진 결정

새 대화에서 이어받을 때 **가장 먼저 읽어야 할 부분**입니다. 모두 이유가 있어서 바꾼 것이니 되돌리지 마세요.

### 2.1 쉬운 문제는 마스터로 세지 않는다

자신감용 2·5·9단 24개는 `m=2`에서 시작해 두 번만 맞히면 마스터가 됩니다. 그대로 두면 별이 첫 주에 24개 쌓여 캐릭터가 한꺼번에 열립니다.

→ `engine.js`의 `isRewardable(key)`가 판정합니다. 마스터리는 정상적으로 오르되 **별·스티커·`totals.mastered`에서 제외**됩니다. 판정은 `applyResult()` 안 한 곳에만 있습니다.

### 2.2 글자 정답의 오답은 같은 묶음에서 뽑는다

설계서 5.3은 숫자 정답만 다룹니다. 글자 정답은 규칙으로 만들 수 없어 **같은 `묶음`의 다른 정답**을 보기로 씁니다. 같은 묶음에 문제가 4개 미만이면 그 문제는 빠지고 부모 화면에 경고가 뜹니다.

→ 부모가 문제를 만들 때 묶음이 오답 품질을 결정합니다. `문제-만들기-안내.md`에 굵게 적혀 있습니다.

### 2.3 오답 생성 세부 규칙

- 답이 10 미만이면 ±1, ±2만 씁니다. 자릿수 바꿈(3→30)은 티가 너무 납니다
- **문제에 이미 등장하는 숫자는 보기에서 제외**합니다 (`6 ÷ 2`의 보기에 6이나 2가 나오면 안 됨)
- 나눗셈은 힌트를 자동 생성합니다 → "곱셈으로 바꿔봐. 2 × ? = 6"

### 2.4 조사 자동 보정

응원 메시지의 `{정답}` 치환 후 받침에 맞춰 조사를 고칩니다. "12이구나" → "12구나", "18야" → "18이야".

→ `app.js`의 `hasFinal()` / `fixJosa()`. 숫자는 읽는 소리 기준이며 받침 있는 끝자리는 **0·1·3·6·7·8**입니다(영·일·삼·육·칠·팔·십). 부모가 메시지를 쓸 때 조사를 신경 쓰지 않아도 됩니다.

### 2.5 v1 이관은 자동이 아니라 붙여넣기

설계서 10장은 `localStorage` 자동 변환을 전제하지만, v1이 다른 주소에서 돌던 파일이라 브라우저가 그 저장소를 읽지 못합니다.

→ v1의 `설정 → 기록 백업` 글자를 복사해 `/parent`에 붙여넣습니다. 변환 코드는 `parent.html`의 `fromV1()`.

### 2.6 친구들은 홈에서 발견된다

4단계 직후 캐릭터를 옷장 두 번째 탭에만 넣었더니 아이가 존재 자체를 몰랐습니다.

→ 목장에 **다음 친구를 회색 실루엣**으로 세우고 "별 8개를 모으면 고양이를 만날 수 있어"를 띄웁니다. 별이 충분하면 홈에 노란 버튼이 하나 더 생깁니다. 친구들은 독립 화면(`view === 'friends'`)이고, 옷장은 입히기만 남았습니다.

### 2.7 출제 후보가 8개는 되도록 보장한다

설계서 7.2의 "앞에서 3개만"을 그대로 적용하니, 팩 하나를 골랐을 때 후보가 3개뿐이라 20문제 중 같은 문제가 8번까지 나왔습니다.

v1에서 이 규칙이 잘 통한 이유는 쉬운 문제 24개가 늘 섞여 있었기 때문입니다. **3은 새로 배우는 것의 개수였지 출제 풀 크기가 아니었습니다.** 팩 필터가 그 완충을 없앴습니다.

→ `engine.js`: `NEW_AT_ONCE = 3`은 그대로 두고, 필터를 적용한 뒤 후보가 `MIN_POOL = 8`에 못 미치면 다음 문제를 순서대로 미리 엽니다. 구구단이나 "다 섞어서"에서는 후보가 이미 충분해 창이 3으로 유지됩니다 — v1 동작 그대로입니다.

추가로 **최근 3문제는 출제에서 제외**합니다(그러면 낼 게 없어지는 경우에만 직전 문제만 제외).

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 다 섞어서 | 19종 / 최다 2번 | 19종 / 최다 2번 |
| 팩 하나 선택 | **3종 / 최다 8번** | 8종 / 최다 4번 |

### 2.8 아이템은 앵커로 붙인다 — 배율까지 (2026-08-19 갱신)

아이템 SVG는 **양 기준 좌표 한 벌**만 있습니다(모자 100,30 / 목 100,96). 캐릭터마다 앵커점으로 옮기고, **앵커점을 중심으로 배율만큼 키웁니다.**

```js
anchor: { hat: [100, 46, 1.28], neck: [100, 111, 1.26] }   // 세 번째 값이 배율, 없으면 1
```

배율은 넷을 다시 그리면서(11장) 생겼습니다. 머리를 키웠더니 같은 왕관이 챙 없는 모자처럼 보였습니다.

`place()`가 `translate(앵커) scale(배율) translate(-양기준)` 한 줄로 처리합니다. **순서를 바꾸면 원점 기준으로 커져서 아이템이 화면 밖으로 날아갑니다.**

→ 새 캐릭터를 추가할 때 아이템 코드는 건드리지 말고 `CHARACTERS`에 앵커 세 값만 넣으세요.
→ 양은 배율이 없어 v1과 픽셀 단위로 같습니다.

### 2.9 끄기는 회수하지 않는다 (5A)

부모가 문제를 꺼도 **앞으로 출제되지 않을 뿐**입니다. 다음은 전부 유지합니다.

`facts`의 진도 · 이미 받은 별 · 도감 스티커 · `totals.mastered` · 해금한 캐릭터 · 열린 상점 단계

마스터한 문제를 끌 때 `totals.mastered`를 다시 계산하면 값이 줄고 → 상점 단계가 닫히고 →
**아이 화면에서 어제까지 살 수 있던 물건이 사라집니다.** 원칙 2.1(차감 금지)에 정면으로 걸립니다.

→ `app.js`는 `P.totals.mastered++`로 누적만 합니다. `facts`에서 재계산하는 코드를 만들지 마세요.
(`parent.html`의 `fromV1()`만 예외 — v1 이관은 어차피 덮어쓰기입니다.)

### 2.10 제외 필터는 `buildIndex()`에서 건다 (5A)

`engine.js`의 `buildIndex(packs, disabled)`가 색인을 지을 때 꺼진 것을 빼버립니다.
그 뒤의 `openKeys` · `poolSize` · `pickKey`는 **손대지 않았습니다.** 2.7의 `MIN_POOL = 8` 보정이
줄어든 문제 수 안에서 그대로 동작합니다. 엔진 입장에서는 "원래 그만큼인 팩"입니다.

부모가 실수로 전부 끄면 아이 화면이 빈 채로 멈추므로, 색인이 비면 **제외를 무시하고 다시 짓습니다.**

> 설계 검토 때는 서버에서 거르려 했지만, 오답 보기가 `packs.py`에서 이미 만들어져 오기 때문에
> 클라이언트에서 걸러도 오답 품질이 나빠지지 않습니다. 묶음(`묶음`)도 클라이언트로 오지 않습니다.
> 그래서 `server.py`·`packs.py`를 한 줄도 고치지 않았습니다.

### 2.11 제외 목록은 프로필 안에 둔다 (5A)

`data/settings.json`이 아니라 프로필 JSON의 `disabled` 필드입니다.

- 기존 `PUT /api/profile/{id}`를 그대로 써서 **서버 변경이 0**입니다
- 원자적 저장과 하루 1회 백업이 공짜로 따라옵니다
- 형제자매마다 다른 목록을 갖습니다 (학년이 다르면 이게 맞습니다)

부모 화면은 저장 직전에 프로필을 다시 받아 `disabled`만 얹고 PUT합니다.
아이가 놀고 있는 중이라도 진행이 덮이지 않게 하려는 것입니다.

### 2.12 `isRewardable`이 없어서 부모 화면이 죽어 있었다 (5A에서 고침)

`parent.html`이 `import { isRewardable } from '/engine.js'`를 하는데 `engine.js`에 그 export가
없었습니다. ES 모듈은 링크 단계에서 실패하므로 **부모 화면 스크립트 전체가 실행되지 않았습니다.**

→ `engine.js`에 `isRewardable(key)`를 추가했습니다. 2.1의 판정을 함수로 노출한 것이고,
`applyResult()` 안의 `entry.rewardable`과 같은 뜻입니다.

---

## 3. 실제 데이터 스키마

설계서 4.1에서 **추가된 필드**만 적습니다.

```jsonc
"daily": { "day": "2026-08-18", "solved": 0, "goal": 20, "speedRuns": 0,
           "right": 0, "grass": 0, "star": 0 },   // right·grass·star 추가
"history": [ { "day": "...", "solved": 20, "right": 17, "grass": 108, "star": 1 } ],

// 5A에서 추가 — 부모가 끈 것. 아이가 처음 들어올 때 없으면 빈 값으로 만들어집니다
"disabled": { "problems": ["나눗셈:q7"], "packs": ["3학년-분수"] },

// 5A에서 추가 — 문제마다 최근 결과. 최신이 앞, 최대 10개, 14일 지나면 버림
"facts": {
  "gugudan:7x8": { "m": 1, "seen": 9, "right": 3,
                   "log": ["0819:x", "0818:h", "0817:o"] }
  //  x 오답  /  h 힌트 쓰고 정답  /  o 혼자 정답
}
```

`seen`·`right`는 누적값이라 "요즘 어려워하는 문제"를 못 뽑습니다. 그래서 창(window)이 있는
`log`를 따로 답니다. `MMDD`에 연도가 없으므로 **오늘보다 미래로 읽히면 작년으로 봅니다** —
이 보정을 빼면 12월 31일에 1월 기록을 내년으로 보고 영영 안 지웁니다.

날짜가 바뀌면 `rollDay()`가 어제 `daily`를 `history`에 넣고(최근 90일) 초기화합니다.

문제 키는 `{팩id}:{공백 제거한 문제 텍스트}`입니다. 내장 구구단만 `gugudan:3x4` 형식입니다.

---

## 4. 실제 API

| 메서드 | 경로 | 비고 |
|---|---|---|
| GET | `/api/bootstrap` | 프로필 목록 + 설정 + 팩 메타 + **메시지** |
| GET | `/api/pack/{id}` | 팩 하나의 문제 전체 |
| GET | `/api/profile/{id}` | |
| PUT | `/api/profile/{id}` | |
| POST | `/api/profile/{id}` | **sendBeacon용** — PUT을 못 보내서 열어둠 |
| POST | `/api/profile` | 새 프로필 |
| GET | `/api/parent/packs` | 팩 목록 + 파싱 경고 + 메시지 경고 (PIN) |
| PUT | `/api/parent/settings` | (PIN) |

**API는 5A에서 하나도 늘지 않았습니다.** 부모 화면이 기존 엔드포인트만으로 다 합니다.

| 부모 화면이 필요한 것 | 어디서 |
|---|---|
| 전체 문제 목록 | `/api/bootstrap` + `/api/pack/{id}` → `buildIndex()` |
| 아이 진도·최근 기록 | `/api/profile/{id}`의 `facts` |
| 최근 7일 | 같은 프로필의 `history` + `daily` |
| 제외 목록 저장 | `PUT /api/profile/{id}` |

`/api/parent/summary`는 만들지 않았고, 앞으로도 필요 없습니다.

---

## 5. 파일 구조

```
server.py        HTTP + 프로필 저장 (표준 라이브러리만)
packs.py         CSV/MD 파서, 오답·힌트 자동 생성, 메시지 파서
web/
  index.html     껍데기
  app.js         화면·상태·저장  (가장 큼)
  engine.js      출제·마스터리·문제 인덱스
  characters.js  캐릭터 5종 SVG, 아이템, 앵커+배율
  styles.css
  parent.html    부모 화면 (현황·약한 문제·문제 켜고 끄기·팩 목록·설정·v1 이관)
content/
  problems/*.csv|md   부모가 넣는 문제 (요청마다 스캔)
  messages.csv        응원 메시지
data/            자동 생성. git에 올라가지 않음
```

앞으로 생길 것 — `ai.py`(6B) · `content/wishes.csv`(5B) · `data/ai-log.jsonl`(6B).
`test/`는 지금 저장소에 없습니다(6장).

`app.js`가 계속 가장 큽니다. 6A에서 유형 분기가, 6C에서 쓰기 화면이 더 들어갑니다.
`renderQuiz()` 하나에 세 유형을 다 넣지 말고 **가운데 답하는 부분만** 갈아 끼우세요 —
`topbar`·`qhead`·`bubble`·`feedback`은 공유합니다(9.2).

---

## 6. 검증 방법

브라우저 없이 실제 플레이를 돌려볼 수 있습니다. `jsdom`으로 DOM을 만들고 `app.js`를 그대로 import한 뒤 버튼을 클릭시키는 방식입니다.

```js
const dom = new JSDOM('<div class="wrap" id="app"></div><div class="toast" id="toast"></div>',
  { url: 'http://127.0.0.1:8770/', runScripts: 'outside-only', pretendToBeVisual: true });
// window.fetch를 서버로 연결하고 globalThis에 document/localStorage/navigator 주입
await import('./web/app.js?v=' + Date.now());
app.querySelector('#play').click();
```

지금까지 이 방식으로 잡은 것: 별 조기 고갈, 조사 오류, 한 자리 답의 부자연스러운 오답,
캐릭터 발견 경로 없음, **부모 화면이 통째로 죽어 있던 것**(2.12).

> ⚠️ **`test/`에는 지금 `t_book.mjs`(도감, 30개)만 있습니다.** 아래 둘은 5A 작업 중에 돌던 것인데
> 커밋되지 않았고, **복구하지 않고 진행하기로 했습니다(2026-08-19).** 아래 목록은 무엇을 확인해야 하는지의
> 기록으로 읽으세요 — 그 항목들은 손으로 봐야 합니다.
>
> 특히 **"회수 없음"** 은 원칙 2.9의 유일한 자동 안전망이었습니다. 상점 단계·별·스티커를 건드리는 작업(5B)에서는
> 마스터한 문제를 하나 끄고 아이 화면을 열어 **아무것도 안 줄었는지 눈으로** 확인하세요.

**5A에는 돌아가는 검증 스크립트가 있었습니다.** `test/` 폴더:

```
node test/t_engine.mjs    # 27개 — 제외 필터, 로그, 연말 넘김, MIN_POOL 보존
node test/t_app.mjs       # 53개 — 가짜 서버 위에서 아이 화면·부모 화면 실제 구동
```

`t_app.mjs`는 `parent.html`의 `<script type="module">` 본문을 꺼내 직접 import합니다
(jsdom이 모듈 스크립트를 실행하지 않기 때문). `characters.js`가 필요하니 실제 파일이 있는
`web/` 옆에서 돌리세요.

`t_app.mjs`에서 가장 중요한 것은 **"회수 없음"** 묶음입니다. 마스터한 문제를 껐을 때
별·풀·`totals.mastered`·스티커·해금·상점 단계가 하나도 안 줄어드는지 봅니다(2.9).

**장기 시뮬레이션은 저장소에 있습니다.**

```
node sim/60days.mjs        # 60일. 인자로 일수를 바꿀 수 있습니다
```

`web/engine.js`와 `packs.py`를 **그대로** 씁니다 — 게임과 같은 파서·같은 출제 엔진이 아니면 볼 의미가 없습니다.
문제 파일이 바뀌면 결과도 바뀌니, 부모가 팩을 넣은 뒤에도 다시 돌려보세요.

**새 보상 규칙을 넣을 때는 반드시 이것을 먼저 돌리세요.** 확인할 값은 두 개입니다 —
별 잔고가 0에 닿는 날이 90일 이후인가, 풀 잔고가 계속 증가하지만 않는가.
스크립트가 상단에 **도달 불가능한 상점 단계 조건**도 같이 경고합니다.

---

## 7. 알려진 한계 (5B에서 해결)

- **풀이 남습니다.** 60일이면 6,000 이상 모이는데 전체 아이템 총액은 2,430입니다. → **해결책 확정**: 전용 아이템 15개로 8,930까지 올립니다(12장, 기획서 8.7). 별 소비처는 배경이 맡습니다
- **도감이 없습니다.** `collection.stickers`에 값은 쌓이는데 보여주는 화면이 없습니다
- **캐릭터 전용 아이템이 없습니다.** 기획서 4.2의 "토끼 귀 리본" 같은 것
- 응원 메시지의 `오랜만` 상황이 아직 발동하지 않습니다 (3일 이상 쉬었는지 판정 미구현)
- 아이 화면에 별을 어디에 쓰는지 안내가 친구들 화면에만 있습니다

5A에서 남긴 것:

- **`log`는 오늘부터 쌓입니다.** 부모 화면의 "요즘 어려워하는 문제"는 며칠 놀아야 채워집니다. 빈 목록은 정상입니다
- 부모가 저장하는 순간 아이가 놀고 있으면 그 세션의 마지막 몇 문제가 덮일 수 있습니다. 저장 직전에 프로필을 다시 받아 창을 좁혔지만 0은 아닙니다. 아이가 안 할 때 만지는 편이 안전합니다
- 팩 안에서 몇 개만 남기면 반복 경고가 뜨지만 막지는 않습니다. 부모 판단에 맡깁니다

---

## 8. 다음에 할 일

5B — 별 소비처와 도감. 순서 제안:

1. ~~**도감 화면**~~ — 완료 (14장)
2. ~~**상점 단계 개방 + 아이템 채우기**~~ — 완료 (15장)
3. ~~**배경 10종 + 소원권 ⭐**~~ — 완료 (16장)
4. ~~**용돈 🌿**~~ — 완료 (17장)

### 소원권과 용돈 — 화폐를 바꿔 달지 마세요

실물 보상은 고갈되지 않는 소비처라 3개월 이후까지 게임이 안 끝납니다. 다만 붙이는 화폐가 원칙을 가릅니다.

| | 화폐 | 이유 |
|---|---|---|
| 소원권 | ⭐ 별 | 드물고 사건이어야 함. 관계형 보상이라 비교·환산이 안 됨 |
| 용돈 | 🌿 풀 | 풀은 '푼 것'에 주므로 **틀려도 용돈이 나옵니다**. 원칙 2.2 |

용돈을 별로 사게 하면 별은 마스터(=정답)에만 붙으므로 "틀리면 돈을 못 번다"가 됩니다.
이 게임이 통한 이유가 정확히 그 반대입니다.

소원권은 `content/wishes.csv`로 부모가 등록하는 구조를 권합니다(팩·메시지와 같은 패턴).
용돈은 환율 + **주 1회 정해진 요일에만** 교환. 매일 환전되면 임금이 되고, 임금이 되면
놀이가 노동이 됩니다(원칙 2.5).

### 별 페이싱 — 지금 그대로 두면 60일에 끝납니다

60일 시뮬레이션의 별 84는 **마스터 보상만** 센 값입니다. 5B의 나머지 획득 경로를 붙이면
수입 약 139, 소비 약 156인데 **소비가 앞에 몰려 있습니다.** 캐릭터 5종이 45일이면 다 열리고,
배경 몇 개를 사면 55~60일에 별 쓸 곳이 사라집니다.

**캐릭터 해금가는 건드리지 마세요.** 목장에 회색 실루엣으로 "별 8개를 모으면 고양이를
만날 수 있어"가 이미 떠 있습니다. 아이에게 한 약속입니다. 소원권으로 뒤를 늘리세요.

넣기 전에 6장의 60일 시뮬레이션으로 균형을 확인하세요.

---

## 9. 확장(6A~6D) 검토 결과

기획서 12~15장을 실제 코드에 비추어 본 결과입니다. **아직 구현 전이고, 여기 적힌 것은 결정이지 기록이 아닙니다.**

### 9.1 좋은 소식 — 바꿀 곳이 생각보다 적습니다

| | 손댈 곳 |
|---|---|
| 단답형 | `packs.py`(유형 열) · `app.js`(입력 화면) · `engine.js`(채점 함수). **`server.py` 0줄** |
| 사진 등록 | `ai.py`(신규) · `server.py`(라우트 3줄) · `parent.html`. **아이 화면 0줄** |
| 서술형 | 위 + `app.js`(쓰기 화면) · 스키마 `writings` |

특히 다음 셋이 이미 맞게 만들어져 있어 그대로 씁니다.

- **글자 정답의 오답 생성**(`auto_wrongs`의 글자 분기, 2.2) — 영어 단어 팩이 이 코드로 바로 돕니다. 새로 만들 것이 없습니다
- **`public_settings()`가 화이트리스트**(`server.py`) — API 키가 아이 화면으로 새지 않는 구조가 이미 있습니다. 전개 연산자로 바꾸지 마세요
- **`applyResult()`의 세 결과**(정답/힌트정답/오답) — 단답형의 모든 경우가 이 셋 중 하나로 들어갑니다. 새 분기가 필요 없습니다

### 9.2 지금 코드에서 실제로 걸리는 곳

**`build_problems()`가 보기를 못 만들면 문제를 통째로 버립니다.**

```python
if len(choices) < 3:
    warnings.append(...)
    continue          # ← 여기
```

단답형에는 이럴 이유가 없습니다. 유형이 `short`면 보기 없이 싣고 「보기 보여줘」 버튼만 감춥니다. 서술형은 아예 보기를 만들지 않습니다.

**`parse_csv()`가 정답 없는 줄을 버립니다.**

```python
if not question or not answer:   # 서술형은 정답이 없어도 됨
```

여기를 놓치면 **서술형 문제가 전부 경고로 사라집니다.** 6C에서 제일 먼저 확인할 곳입니다.

**`renderQuiz()`는 `q.choices`가 있다고 가정합니다.** 유형 분기를 넣을 때 `topbar` · `qhead` · `bubble` · `feedback`은 그대로 재사용하고 **가운데 답하는 부분만** 갈아 끼우세요. 이 함수가 지금도 큰 편이라, 유형별로 통째로 복사하면 세 벌이 됩니다.

**`answer(picked)`가 `b.textContent`로 값을 읽습니다.**

```js
app.querySelectorAll('[data-pick]').forEach(b => b.onclick = () => answer(b.textContent));
```

입력창에서 부를 때는 `data-pick`이 없습니다. `answer(value)`로 값을 직접 넘기는 형태는 이미 되어 있으니, **입력 경로도 같은 함수를 부르게** 하세요. 채점·보상·저장·마스터리가 전부 여기 모여 있어서, 두 번째 경로를 만들면 그때부터 어긋납니다.

### 9.3 확실히 결정한 것

| 질문 | 결정 | 이유 |
|---|---|---|
| 단답 정답 보너스를 더 줄까 | **아니오, 동일** | "쓰면 더 준다"는 쓰는 것을 부담으로 만듦 (원칙 2.2) |
| 「보기 보여줘」를 몇 번까지 | **제한 없음** | 힌트가 무료인 것과 같음 (원칙 2.3) |
| 한글 조사를 자동 보정할까 | **아니오** | "빨래"를 "빨"+조사로 자르는 사고가 더 나쁨. 복수 정답(`/`)으로 적게 함 |
| AI가 조언을 해도 될까 | **예 — 선생님 페르소나로** | 2026-08-19 부모 관찰. 9.5 참고 |
| 조언을 몇 개까지 | **하나** | 두 개면 조언이 아니라 지적 목록이 됨 |
| 조언을 끌 수 있어야 하나 | **예, 부모 스위치. 기본 켜짐** | 근거가 관찰이고 관찰은 바뀜 |
| 선생님을 목장 식구로 넣을까 | **아니오** | 친구는 "나도 헷갈려", 선생님은 "이건 이래서 그래". 섞으면 둘 다 죽음 |
| 서술형에 별을 줄까 | **내용으로는 아니오** | 이야기 판(횟수)으로만. AI 판정이 별로 환산되면 원칙 2.2가 무너짐 |
| 서술형 하루 몇 개 | **2개** | 부담이 큼. 그 이상은 부모가 자유 모드로 지정할 때만 |
| OCR을 브라우저에서 할까 | **아니오, Gemini vision** | Tesseract는 wasm 수 MB + 한영 혼합 정확도가 나쁨. 어차피 키가 있음 |
| 인식 결과를 자동 저장할까 | **절대 아니오** | 틀린 단어가 나가면 아이는 *맞게 쓰고 틀렸다*를 배움 |
| AI 응답을 기다릴까 | **아니오** | 보상·다음 버튼이 먼저. 응답은 말풍선만 나중에 바꿈 |

### 9.4 남은 위험

**아이 이름 유출.** `messages.csv`의 `{이름}` 치환자가 이미 있습니다. 서술형 프롬프트를 만들 때 치환된 문장을 실어 보내면 아이 이름이 Google로 갑니다. **프롬프트는 치환 전 원문으로 만들고, 프로필 객체를 `ai.py`에 넘기지 마세요.**

**별 페이싱.** 이야기 판은 별 **공급처**입니다. 하루 2문제면 12칸 판 하나에 6일, 60일에 별 30입니다. 5B의 소원권으로 뒤를 늘리기로 한 계산에 이만큼이 더 들어옵니다. **6C를 넣기 전에 60일 시뮬레이션을 다시 돌리세요**(6장).

**프로필 JSON 크기.** `writings`가 처음으로 프로필을 계속 키우는 필드입니다. 100개 상한을 처음부터 겁니다. `MAX_BODY`는 1MB이고, 넘으면 `read_body()`가 조용히 잘라서 **JSON 파싱 오류로 저장이 통째로 실패**합니다.

**무료 등급의 데이터 취급.** 아이가 쓴 글이 모델 개선에 쓰일 수 있습니다. 이건 코드로 못 막습니다. 부모가 알고 켜는 것 외에 방법이 없으므로, 그 문구를 설정 화면에서 **접어두지 말고** 항상 보이게 두세요.

---

## 10. 결정 변경 기록 — AI가 조언을 합니다 (2026-08-19)

이 문서에서 **원칙이 완화된 유일한 사례**입니다. 되돌리기 전에 여기를 읽으세요.

### 10.1 무엇이 바뀌었나

처음 설계한 원칙 2.10은 **AI가 조언을 일절 못 하게** 했습니다. 반응만 하고, 빠진 것이 있어도 정보로만 흘리고, 가르치지 않는 구조였습니다. 어른의 지적이 아이를 다시 조용하게 만들까 봐서였습니다.

부모가 근거를 가지고 뒤집었습니다.

> 아이가 자신감이 붙었습니다. 그리고 **부모의 지적은 부끄러워하지만, 선생님의 지시는 기꺼이 따르고 오히려 안정감을 얻습니다.**

이건 이 아이에 대한 직접 관찰이고, 문서상의 설계보다 강한 근거입니다.

### 10.2 왜 이 완화가 원칙을 안 깨나

핵심은 **누가 말하느냐**입니다. 같은 문장도 부모가 말하면 지적이 되고 선생님이 말하면 배움이 됩니다. 아이가 이미 그 구분을 가지고 있습니다.

그래서 완화한 것은 "조언 금지" 하나뿐이고, **아래 셋이 그대로라서 성립합니다.**

| 그대로인 것 | 왜 이게 조언을 안전하게 만드나 |
|---|---|
| 서술형에 점수·마스터리·별이 없음 | 선생님 말이 보상에 **영향을 줄 수 없음**. 아이 입장에서 조언은 *잃을 것이 없는 정보* |
| 보상이 조언보다 **먼저** 지급됨 | 이미 받은 다음에 듣는 말이라 판정으로 읽히지 않음 |
| 선생님이 친구들과 **분리**됨 | "어른도 헷갈린다"(2.9)를 보여주는 다섯은 그대로 남음 |

**채점과 조언을 같이 붙이면 조언이 감점 사유로 읽힙니다.** 이 셋 중 하나라도 무너지면 조언을 다시 막아야 합니다.

### 10.3 되돌리는 순서

관찰이 근거이므로 관찰이 바뀌면 되돌립니다. 한 번에 다 끄지 말고 순서대로.

```
1. 조언 스위치 끄기        선생님은 남고 말만 짧아짐 — 아이는 거의 못 느낌
2. AI 끄기                 선생님이 준비된 문장만 말함
3. 서술형 자체를 끄기       마지막 수단
```

되돌릴 신호는 **서술형 글자 수**입니다. 조언을 켠 뒤 아이가 쓰는 양이 줄면 선생님 말이 지적으로 읽히고 있다는 뜻입니다. `writings`에 날짜와 글이 다 있으니 세어볼 수 있습니다.

세션 이탈이나 서술형 회피가 보이면 그때는 이미 늦은 편입니다. 글자 수가 먼저 움직입니다.

### 10.4 이번 결정으로 늘어난 작업

| | 무엇 |
|---|---|
| `characters.js` | **선생님 SVG 1종**(부엉이 권장). 앵커 불필요 — 꾸미지 않습니다 |
| `packs.py` | `CHAR_NAMES`에 `선생님`, `SITUATIONS`에 `글쓰기` |
| `content/messages.csv` | `선생님,글쓰기,...` 6줄 이상 |
| `ai.py` | 프롬프트 선생님 페르소나 + `{조언}` 자리 |
| `data/settings.json` | `ai.advice`, `ai.teacherName` |
| `parent.html` | 조언 스위치, **조언 문장만 모아 보는 목록** |

선생님 SVG는 앵커를 안 넣습니다. 아이템을 못 입히는 것이 의도입니다 — 꾸밀 수 있으면 목장 식구가 되고, 목장 식구가 되면 역할이 섞입니다(기획서 4.3).

### 10.5 남은 위험

**금지어 목록으로는 못 막습니다.** `BANNED`를 피해 가는 지적 문장은 얼마든지 만들어집니다 — "이 부분은 조금 더 생각해보면 좋겠어" 같은 것. 검열은 최후의 그물이고, **진짜 확인은 부모가 로그의 조언 문장을 읽는 것**입니다. 그래서 부모 화면의 조언 목록이 선택 기능이 아닙니다.

**모델이 바뀌면 말투가 바뀝니다.** `ai.model`을 고친 다음에는 조언 문장 열 줄을 반드시 읽어보세요. 같은 프롬프트가 모델에 따라 훨씬 훈계조로 나올 수 있습니다.

**친구 캐릭터가 오염될 수 있습니다.** 선생님 대사를 쓰다 보면 `messages.csv`의 친구 대사도 가르치는 톤으로 흘러갑니다. 친구들은 끝까지 **"나도 이거 자꾸 헷갈리더라"** 쪽이어야 합니다. v1이 통한 이유가 그것뿐입니다.

---

## 11. 캐릭터를 다시 그렸습니다 (2026-08-19)

**코드가 실제로 바뀐 유일한 항목입니다.** 위 9·10장은 계획이지만 이건 `web/characters.js`에 반영돼 있습니다.

### 11.1 무엇이 문제였나

부모 관찰: **"양을 제외한 캐릭터들의 미형이 너무 부족하다."**

원인은 그림 실력이 아니라 **호환을 위해 그림체를 맞춘 것**이었습니다. 넷 다 `ellipse cx=100 cy=112 rx=46 ry=40` 몸통에 같은 `legs()`, 같은 `eyes()`를 쓰고 색만 달랐습니다. 아이템 앵커가 위치만 옮길 수 있어서 머리 크기를 캐릭터마다 다르게 할 수 없었고, 그래서 다 비슷한 체형이 됐습니다.

### 11.2 배율이 제약을 풀었습니다

앵커에 세 번째 값(배율)을 더하니 **그림체를 자유롭게 하면서 아이템 한 벌을 계속 공용으로 쓸 수 있게** 됐습니다(2.8). 아이템 SVG는 한 글자도 안 고쳤습니다.

### 11.3 넷에 적용한 비례

| | 이전 | 지금 |
|---|---|---|
| 얼굴 `rx` | 34~37 | **40~41** |
| 몸통 `rx` | 46 | 36~40 |
| 눈 반지름 | 5.6 | **9** |
| 눈 위치 | 얼굴 한가운데 | **얼굴 아래쪽 1/3** |
| 하이라이트 | 1개 | **2개** (큰 것 + 작은 것) |
| 다리 | `rect` 4개 | **앞발 2개** |
| 볼터치 | 토끼만 | **전원** |

**눈을 얼굴 아래로 내린 것이 가장 큰 차이**입니다. 아기 얼굴의 핵심 비례이고, 이것만 고쳐도 인상이 달라집니다.

새 헬퍼는 `paws` · `bigEyes(mood, y, r, sx)` · `sleepyBig(y, sx)` · `cheeks` · `smile`입니다. `sx`는 눈 사이 벌어짐이고, 코가 큰 코알라는 24까지 벌려 코 자리를 만듭니다.

**양은 손대지 않았습니다.** `legs` · `eyes` · `blush`가 양 전용으로 남아 있습니다. 지우지 마세요.

### 11.4 정체성은 비례보다 우선합니다

- 카피바라의 네모난 얼굴은 모서리 `rx`만 31로 굴리고 유지했습니다. 둥글게 만들면 귀여워지지만 **카피바라가 아니게 됩니다**
- 고양이 꼬리는 굵은 선 두 겹(테두리색 15 + 몸색 11)으로 말아 올렸습니다
- 토끼 앞니는 턱선 아래로 2px 삐져나옵니다. 의도한 것입니다
- 코알라의 `z z`는 평소 얼굴일 때만 나옵니다

### 11.5 확인 방법

브라우저 없이 렌더링만 검사할 수 있습니다.

```bash
cp web/characters.js /tmp/ch.mjs   # node가 .js를 CJS로 읽어서 확장자를 바꿔야 합니다
node --input-type=module -e "import('/tmp/ch.mjs').then(m=>{
  for (const c of m.CHARACTERS) for (const mood of ['','happy']) {
    const s = m.charSVG(c.id, {hat:'crown', neck:'rain'}, mood);
    console.log(c.id, mood||'plain', s.startsWith('<svg') && !s.includes('NaN') ? 'OK' : 'BAD');
  }})"
```

`NaN`이 섞이는 것이 이 파일에서 가장 흔한 사고입니다 — 좌표 계산에 `undefined`가 들어가면 SVG가 조용히 안 그려집니다.

눈으로 볼 때는 **이전과 지금을 나란히 놓는 비교 페이지**를 만드세요. `characters.js`를 두 벌 인라인해서 같은 화면에 그리면 됩니다. 배율이 맞는지는 모자 7종·목도리 6종을 다섯에 전부 씌워봐야 압니다.

---

## 12. 아이템은 공용을 유지합니다 (2026-08-19 결정)

기획서 8.7의 근거 기록입니다.

### 12.1 검토한 것

**"캐릭터마다 악세서리를 따로 사게 할까?"** 풀 소비처가 다섯 배(2,430 → 12,150)가 되어 7장의 "풀이 남는다"가 단번에 해결됩니다.

**결론: 하지 않습니다.** 부작용이 이득보다 큽니다.

| 부작용 | 왜 치명적인가 |
|---|---|
| 캐릭터를 안 바꾸게 됨 | 왕관 씌운 양에서 맨머리 고양이로 바꾸지 않습니다. **5종을 만든 이유가 사라집니다** |
| 해금이 빈손으로 시작 | 새 친구가 아무것도 못 입은 채 등장합니다. 기획서 4.1의 "늘어난 식구" 순간이 약해집니다 |
| 회수처럼 느껴짐 | "어제 산 왕관인데 고양이한테는 없다". 원칙 2.1의 정신에 걸립니다 |

### 12.2 대신 전용 아이템

캐릭터당 모자·목도리·소품 1개씩, **15개**를 상점 3~5단계에 넣습니다. 총액 2,430 → **8,930**.

개별 구매가 노리던 소비처 확대를 부작용 없이 얻고, **"이 친구를 데리고 다녀야 할 이유"** 까지 생깁니다. 개별 구매가 만들려던 것이 사실은 이것이었습니다.

### 12.3 구현할 때 지킬 것

- **`inventory.items` 구조를 바꾸지 마세요.** 누구 것인지는 아이템 정의의 `only`에 있으므로 프로필에 다시 적을 필요가 없습니다. 저장 형식을 바꾸면 마이그레이션이 필요하고, 그게 실패하면 **아이가 몇 주 모은 물건이 사라집니다**
- 아직 못 만난 친구의 전용 아이템은 **상점에 안 보입니다.** 다음 목표는 홈 화면 실루엣이 이미 보여주고 있습니다(2.6). 두 곳에서 같은 일을 하면 상점이 잠긴 물건 창고가 됩니다
- 전용 아이템 미리보기는 **그 캐릭터에 씌워서** 보여주세요. 활동 캐릭터에 씌우면 못 입는 물건이 입은 것처럼 보입니다
- 옷장에서 안 보이는 것은 사라진 것이 아닙니다. 그래도 헷갈리므로 *"토끼만"* 같은 표시를 붙이세요
- `isHat(id) ? 'hat' : 'neck'` 두 갈래가 소품 때문에 세 갈래가 됩니다. **`slotOf(id)` 한 곳으로 모으세요**

### 12.4 남은 것

전용 아이템 SVG 15개를 아직 안 그렸습니다. 5B에서 상점 단계 개방과 같이 넣습니다.

전용은 **그 캐릭터가 아니면 어색해야** 전용인 이유가 있습니다. 카피바라 머리 위의 귤, 토끼 귀에 묶는 리본처럼 앵커가 그 캐릭터에서만 말이 되는 것으로 고르세요. 공용 아이템을 색만 바꿔 전용이라고 하면 아이가 금방 알아챕니다.

---

## 13. 5B 착수 전 시뮬레이션 (2026-08-19)

`node sim/60days.mjs`를 돌린 결과입니다. **5B는 전부 보상 규칙이라 이 숫자가 곧 설계값입니다.**

문제 파일 기준: 구구단 15 + 과학-식물 12 + 나눗셈 24 + 한국사 24 = **별을 주는 문제 75개**
(쉬운 구구단 24개는 마스터로 안 셉니다 — 2.1)

| 일 | 마스터 | 별 번 | 별 잔고 | 친구 | 배경 | 풀 번 | 풀 잔고 |
|---|---|---|---|---|---|---|---|
| 14 | 14 | 23 | 4 | 2 | 3 | 1,321 | 791 |
| 30 | 31 | 49 | 5 | 4 | 4 | 2,925 | 495 |
| 45 | 48 | 78 | 1 | 5 | 6 | 4,425 | 875 |
| 60 | 63 | 107 | 18 | 5 | 7 | 6,066 | 416 |
| 90 | 75 | 149 | **60** | 5 | 7 | 9,427 | 777 |

### 13.1 발견 1 — 상점 5단계가 도달 불가능했습니다

조건이 **80문제 마스터**인데 별을 주는 문제가 **75개뿐**입니다. 부모가 팩을 더 넣지 않으면 영영 안 열립니다.

→ **65로 내렸습니다**(기획서 8.3). 시뮬레이션에서 68일쯤 열립니다.

조건은 **내리는 것만 안전**합니다. 올리면 이미 열린 단계가 닫히고, 어제까지 살 수 있던 물건이 사라집니다(원칙 2.1). 팩이 늘어도 65는 계속 도달 가능합니다.

### 13.2 발견 2 — 별 소비처가 50일쯤 마릅니다

친구 4명 해금(88) + 배경 6종(49) = **137**을 다 쓰고 나면 살 것이 없습니다. 20회 반복에서 평균 **50일**(42~54일), 스크립트 기본 실행에서 53일이었습니다.

문서 8장이 "55~60일"로 예상했는데 **더 빠릅니다.** 90일에는 별 60이 그냥 쌓여 있습니다.

| 대책 | 효과 |
|---|---|
| 배경 5~8종 → **10종 이상** | 며칠 미룸. 결국 마름 |
| **소원권** | 고갈되지 않는 유일한 소비처 |

→ **소원권은 5B의 마지막 항목이 아니라 필수 항목입니다.** 배경만 넣고 끝내면 두 달 뒤 별이 의미를 잃습니다.

### 13.3 발견 3 — 이야기 판(6C)은 이 문제를 악화시킵니다

서술형 하루 2문제로 이야기 판을 채우면 별 소비처가 마르는 날이 **53일 → 41일**로 당겨집니다. 90일 별 잔고는 60 → **105**입니다.

→ **소원권을 먼저 넣고 6C를 하세요.** 순서를 바꾸면 별이 남아도는 상태로 6C를 얹게 됩니다.

### 13.4 발견 4 — 풀은 괜찮습니다

60일에 6,066을 벌어 잔고 416, 90일에 9,427을 벌어 잔고 777입니다. **계속 증가하지 않습니다.** 7장의 "풀이 남는다"는 전용 아이템 15개(12장)로 해결됐습니다.

단, 3~5단계 전용 아이템은 **해금한 친구 수만큼만** 살 수 있어서, 친구가 늦게 열리면 풀이 그만큼 더 남습니다. 시뮬레이션은 그것까지 반영한 값입니다.

### 13.5 그래서 5B 순서를 이렇게 바꿉니다

```
1. 도감 화면          collection.stickers → 12칸 판, 채우면 별 3
2. 상점 단계 개방      tier·only 필드, prop 슬롯, slotOf(id)
3. 전용 아이템 15개    SVG. 안 그리면 개방해도 빈 선반
4. 배경 10종          별 소비처
5. 소원권             ← 4번과 묶어서. 미루면 두 달 뒤 별이 죽습니다
6. 용돈               🌿 풀. 주 1회 정해진 요일에만
```

**4번과 5번을 같이** 하는 것이 이번 시뮬레이션의 결론입니다. 원래 소원권은 5B의 맨 끝이었습니다.

---

## 14. 도감 화면 (5B-1, 2026-08-19 완료)

`collection.stickers`에 값만 쌓이고 보여주는 곳이 없던 것을 채웠습니다(7장).

### 14.1 무엇이 생겼나

- 홈에 **도감** 버튼 (`view === 'book'`). 버튼이 넷이 되어 2×2로 나눴습니다
- 12칸 판. **채우는 중인 판이 맨 위**, 다 채운 판은 아래에 최신순
- 판을 채우면 **별 3** — 기획서 8.4

### 14.2 스티커는 문제마다 색과 기울기가 고정입니다

`hashOf(key)`로 색 6종 중 하나와 −3~+3도 기울기를 정합니다. 무작위로 하면 화면을 다시 열 때마다 스티커가 바뀌어서 **모은 것이 아니라 그때그때 그려지는 것처럼** 보입니다.

라벨은 8자 이하로 맞춥니다. 구구단은 `7×8`, 짧은 문제는 문제 그대로, 긴 문제는 **정답**을, 둘 다 길면 잘라서 `…`.

### 14.3 판 보상은 더하기만 합니다 — `catchUpBoards()`

```js
const done = Math.floor(stickers.length / BOARD_SIZE);
const due  = done - (boardsCompleted || 0);
if (due <= 0) return 0;          // ← 줄어드는 경로가 없다
```

한 함수를 **두 곳**에서 씁니다.

| 어디 | 왜 |
|---|---|
| `answer()`의 마스터 직후 | 판을 채운 그 순간에 별 3 |
| `enter()`의 프로필 진입 | **5B 이전에 모아둔 스티커를 소급** |

소급이 필요한 이유: 도감 보상은 지금 생겼는데 스티커는 4단계부터 쌓이고 있었습니다. 37장을 모아둔 아이는 들어오는 순간 별 9를 받습니다.

`boardsCompleted`가 스티커 수보다 앞서 있어도 **줄이지 않습니다.** `due <= 0`이면 그냥 0입니다(원칙 2.9).

### 14.4 부모가 끈 문제의 스티커도 보입니다

`stickerLabel()`은 `INDEX`에 없는 키도 읽습니다. 키가 `{팩id}:{공백 뺀 문제}`라 뒤쪽을 그대로 쓰면 됩니다. 구구단은 `gugudan:(\d+)x(\d+)`를 정규식으로 풀어 `7×8`로 만듭니다.

**이게 없으면 부모가 문제를 끄는 순간 아이 도감에 구멍이 생깁니다.** 원칙 2.9가 화면에서 깨지는 자리입니다.

### 14.5 토스트는 하나만

마스터 토스트와 판 완성 토스트가 겹칩니다. 정답이면 1.1초 뒤 다음 문제로 넘어가는데 토스트는 1.8초라 두 개를 띄우면 뒤엣것이 잘립니다.

→ 판을 채운 날은 **판 완성 쪽만** 띄웁니다. 더 큰 소식이 이깁니다.

### 14.6 검증

```
node test/t_book.mjs        # 30개
```

`jsdom`이 없어서 순수 함수 네 개(`catchUpBoards`·`stickerLabel`·`hashOf`·`boardHTML`)를 `app.js`에서 정규식으로 떼어 검사합니다. **5B의 나머지도 같은 방식으로 붙이세요** — 보상 로직은 대부분 순수 함수라 DOM 없이 검사됩니다.

특히 이 셋을 봅니다.

- 12장에 별 3, 재호출해도 다시 안 줌
- `boardsCompleted`가 앞서 있을 때 **줄지 않음**
- 색인에 없는 키도 라벨이 나옴

함수 이름을 바꾸면 `grab()`이 "app.js에 …가 없습니다"로 바로 실패합니다. 조용히 통과하지 않습니다.

`node sim/60days.mjs`도 다시 돌렸습니다. 도감 보상은 시뮬레이션이 이미 반영하고 있던 값이라 균형은 그대로입니다(별 소비처 51일에 마름 — 13.2).

### 14.7 남긴 것

- **이야기 판**(6C)이 들어오면 탭이 필요합니다. 지금은 판이 한 종류라 탭을 안 만들었습니다
- 캐릭터 도감(기획서 9장)은 `친구들` 화면과 겹쳐서 만들지 않았습니다
- 판이 많아지면(90일에 6판) 세로로 길어집니다. 접기가 필요해지면 그때 넣으세요

---

## 15. 상점 단계 개방과 전용 아이템 (5B-2, 2026-08-19 완료)

기획서 8.3·8.7의 구현입니다. 아이템이 13종 → **30종**, 총액 2,430 → **9,450풀**이 됐습니다.

### 15.1 무엇이 생겼나

| | |
|---|---|
| `tier` · `only` | 아이템 정의에 두 필드. **둘 다 없을 때가 기본**이라 기존 13종은 한 줄도 안 고쳤습니다 |
| `slotOf(id)` | 슬롯 판정을 한 곳으로. `isHat(id) ? 'hat' : 'neck'`이 세 갈래가 됐습니다 |
| `prop` 슬롯 | 스키마에만 있던 것을 실제로 씁니다. 앵커도 캐릭터마다 새로 넣었습니다 |
| 전용 15종 | 캐릭터당 모자·목도리·소품 하나씩 |
| 공용 소품 2종 | 풍선·그림책 (2단계) |
| `catchUpTier()` | `totals.mastered` → `shopTier`. **올리기만 합니다** |

### 15.2 단계는 내려가지 않습니다

```js
const now = Math.max(before, tierOf(P.totals.mastered));
```

`Math.max`가 전부입니다. `totals.mastered`는 누적만 하지만(2.9), 혹시 줄어드는 경로가 생겨도 **선반은 안 닫힙니다.** 닫히면 어제까지 살 수 있던 물건이 사라집니다.

`P.settings.newTier`에 새로 열린 단계를 적어두고 홈에 알림 버튼을 띄웁니다. 가게에 들어가면 지웁니다. 서버·스키마 변경은 없습니다 — `settings`는 이미 있는 객체입니다.

### 15.3 아직 못 만난 친구의 전용은 가게에 안 보입니다

```js
const buyable = it => (it.tier || 1) <= P.shopTier &&
                      (!it.only || P.characters.unlocked.includes(it.only));
```

다음 목표를 보여주는 일은 목장의 회색 실루엣이 이미 하고 있습니다(2.6). 두 곳에서 같은 일을 하면 **가게가 잠긴 물건 창고**가 되고, 살 수 없는 것이 살 수 있는 것보다 많아집니다.

덕분에 **두 화폐가 서로를 밉니다** — 별로 친구를 만나면 풀 쓸 곳이 세 개씩 늘어납니다. 지금까지 풀과 별은 서로 무관했는데 여기서 처음 맞물립니다.

### 15.4 옷장에서 안 보이는 것은 사라진 게 아닙니다

토끼 리본은 토끼일 때만 목록에 뜹니다. 아이가 헷갈리지 않게:

- 가게에서 전용은 **그 친구에게 씌워서** 보여주고 이름표(*"토끼만"*)를 붙입니다
- 사면 **주인에게 바로 입혀둡니다** — `worn(it.only || active())`
- 다른 친구 전용만 가진 상태면 옷장이 *"다른 친구한테 가보면 있을지도!"* 라고 알려줍니다

### 15.5 소품 앵커는 몸 옆입니다

양 기준 `[148, 146]`. 모자·목도리와 달리 **몸 위에 겹치므로 마지막에 그립니다.**

토끼는 꼬리가 그 자리에 있어 앵커를 아래로(`[150, 157]`) 내렸습니다. 겹침이 남아 있을 수 있으니 눈으로 보세요.

### 15.6 검증

```
node test/t_shop.mjs     # 31개
node test/t_book.mjs     # 30개
node sim/60days.mjs
```

**시뮬레이션이 이제 `characters.js`에서 가격표를 직접 읽습니다.** 가격을 두 곳에 두면 반드시 어긋납니다.

`t_shop.mjs`가 실제로 사고를 하나 잡았습니다 — **아이템 30종이 데이터만 있고 그림이 하나도 없는 상태**로 전 조합 렌더링을 통과했습니다. 빈 문자열도 유효한 SVG라서요. 그래서 *"아이템을 씌운 SVG가 안 씌운 것보다 길어야 한다"* 는 검사를 넣었습니다. 정의만 있고 그림이 없으면 **화면에는 아무 일도 안 일어나면서 풀만 빠져나갑니다.**

### 15.7 시뮬레이션 결과 — 두 목표값 통과

시뮬레이션에도 버그가 있었습니다. 캐릭터 값을 **차액**(8·7·10·15 = 40)으로 계산했는데 실제 `pickChar()`는 매번 **전액**을 뺍니다(8+15+25+40 = 88). 별 소비가 절반으로 잡혀 낙관적으로 나오고 있었습니다.

고치고, 배경을 10종(누적 83)으로 잡은 뒤 12회 평균:

| 90일 | |
|---|---|
| 별 잔고 | **13** — 0에 안 닿음 ✓ |
| 친구 | 4 / 5 |
| 풀 잔고 | 1,471 — 계속 증가하지 않음 ✓ |
| 산 것 | **27 / 30** |

90일에 아직 살 것이 3개 남습니다(카피바라 전용 — 아직 못 만남). 성공 기준 2번 "8주가 지나도 살 게 없다는 상태가 오지 않는다"를 넘겼습니다.

### 15.8 남긴 것

- **전용 아이템 SVG를 눈으로 확인해야 합니다.** 특히 소품 — 처음 쓰는 앵커라 몸·꼬리와 겹칠 수 있습니다
- 배경 10종은 아직 없습니다(5B-3). 시뮬레이션의 별 소비처 절반이 아직 코드에 없는 값입니다
- 5단계까지 다 열려도 `nextShelf()`가 조용해질 뿐, 그 다음 목표는 소원권이 맡습니다

---

## 16. 배경과 소원권 (5B-3, 2026-08-19 완료)

13.2에서 확인한 **별 소비처 고갈**을 막는 작업입니다. 둘을 같이 넣은 이유가 16.4에 있습니다.

### 16.1 배경 — `web/backgrounds.js` (새 파일)

11종(무료 1 + 별 10, 합계 **83별**). 가게에 `배경` 탭이 생겼습니다.

배경은 **CSS 변수 세 개만** 바꿉니다.

```
--meadow       하늘 쪽 (위)
--meadow-deep  땅 쪽 (아래)
--onpage       카드 바깥 글자색   ← 새로 만든 것
```

`--onpage`가 핵심입니다. 밤 배경을 넣으려면 페이지가 어두워지는데, 그러면 카드 **바깥** 글자가 안 보입니다. 그래서 `body{color}`를 `--onpage`로 돌리고 `.card`·`.item`·`.says p`에 `color:var(--ink)`를 못 박았습니다. **카드 안은 어떤 배경에서도 그대로**입니다.

무료 배경(`day`)은 계절·시간에 따라 저절로 바뀝니다(기획서 8.5) — 한밤·이른 아침·해질녘·밤, 그리고 낮은 봄·여름·가을·겨울. 아무것도 안 사도 화면이 늘 같지는 않습니다. `render()`마다 다시 계산하므로 놀다가 저녁이 되면 화면이 넘어갑니다.

장식(별·눈·꽃잎·비·오로라·반딧불)은 `#sky` 고정 레이어에 SVG로 얹습니다. **무작위가 아니라 고정 배치**입니다 — 화면을 다시 열 때마다 별자리가 바뀌면 산만합니다.

### 16.2 소원권 — `content/wishes.csv`

문제 팩·메시지와 **같은 방식**입니다. 파일이 없으면 기능 자체가 아이 화면에 안 나타납니다. 그게 기획서 8장이 말한 "기본 꺼짐"입니다 — 따로 스위치를 만들지 않았습니다.

```csv
소원,별,설명
아이스크림 사러 가기,8,아무 때나 쓸 수 있어
같이 영화 보기,20,팝콘도 같이
```

별 칸이 숫자가 아니면 **그 줄만** 건너뛰고 부모 화면에 경고가 뜹니다. 팩 파서와 같은 규칙입니다.

### 16.3 받은 소원권은 지우지 않습니다

`P.wishes`는 **받은 기록**이지 남은 장수가 아닙니다. "썼다" 표시를 만들지 않았습니다.

이유가 둘입니다.

1. 아이가 실수로 눌러 날릴 수 있고, **사라지는 것은 회수처럼 느껴집니다**(원칙 2.1)
2. 소원권은 관계형 보상입니다. 아이가 화면을 부모에게 보여주는 것이 이 기능의 전부이고, 들어준 뒤에 목록이 지워질 이유가 없습니다

같은 소원을 여러 번 바꿀 수 있습니다. 아이스크림을 두 번 바꾸면 아이스크림 두 개입니다.

부모 화면에 `소원권` 섹션이 생겼습니다 — 걸어둔 소원 목록, 아이가 몇 번 바꿔갔는지, 바꾼 날짜. 아이가 말을 안 해도 부모가 압니다.

### 16.4 시뮬레이션 — 소원권이 없으면 140일에 별이 죽습니다

`node sim/60days.mjs 180`

| | 소원권 없이 | 소원권 포함 |
|---|---|---|
| 별 소비처가 마르는 날 | **140일** | 안 마름 |
| 180일 별 잔고 | 17 (쓸 곳 없음) | 5 |
| 180일 바꾼 소원권 | — | 1장 (6C까지 넣으면 8장) |

13.2에서 "50일에 마른다"고 했던 것이 배경 10종을 넣어 140일까지 밀렸고, 소원권이 그 뒤를 무한히 받습니다. **고갈되지 않는 소비처는 소원권뿐**이라는 8장의 결론이 숫자로 확인됐습니다.

시뮬레이션도 이제 `backgrounds.js`에서 배경 값을 직접 읽습니다. 가격표를 두 곳에 두지 않습니다.

### 16.5 새로 나온 문제 — 180일에 **풀**이 남습니다

| 일 | 풀 잔고 |
|---|---|
| 90 | 1,503 |
| 120 | 4,978 |
| 180 | **10,309** |

아이템 30종을 180일쯤 다 삽니다(`30/30`). 그때부터 풀은 벌기만 하고 쓸 곳이 없습니다. **26장에서 48종으로 늘려 180일 45/48이 됐습니다.**

→ **이것이 용돈(5B-4)이 필요한 정확한 이유입니다.** 8장이 "용돈은 풀"이라고 한 근거가 여기서 숫자로 나왔습니다. 별로 사게 하면 "틀리면 돈을 못 번다"가 되어 원칙 2.2가 무너집니다.

### 16.6 검증

```
node test/t_wish.mjs     # 33개
```

배경 데이터·자동 변화·장식·배경 구매·소원권 교환, 그리고 **"받은 소원권을 지우는 코드가 app.js에 없다"** 를 정규식으로 확인합니다. `wishes.csv` 파서는 `python3`를 불러 같이 검사합니다 — 파일이 없을 때, 별이 숫자가 아닐 때.

### 16.7 남긴 것

- 배경 장식을 **눈으로 확인**해야 합니다. 특히 밤·오로라·반딧불에서 카드 바깥 글자가 읽히는지
- `content/wishes.csv`는 예시입니다. **아이와 이야기해서 바꾸세요** — 집안 사정이 들어갈수록 잘 통합니다
- 용돈(5B-4)이 남았습니다. 16.5가 그 근거입니다

---

## 17. 용돈 (5B-4, 2026-08-19 완료) — 5B 끝

16.5에서 나온 **180일 풀 잔고 10,309**을 받는 소비처입니다.

### 17.1 화폐는 풀입니다

| | 화폐 | 왜 |
|---|---|---|
| 소원권 | ⭐ 별 | 드물고 사건이어야 함 |
| 용돈 | 🌿 **풀** | 풀은 '푼 것'에 주므로 **틀려도 용돈이 나옵니다** |

별로 바꾸게 하면 별은 마스터(=정답)에만 붙으므로 **"틀리면 돈을 못 번다"** 가 됩니다. 이 게임이 통한 이유가 정확히 그 반대입니다.

`t_allow.mjs`가 `exchange()` 본문을 정규식으로 검사합니다 — `wallet.grass`를 빼고 `wallet.star`는 **건드리지 않는지**. 이게 이 기능의 유일한 절대 조건입니다.

### 17.2 설정은 `data/settings.json` (파일이 아니라)

소원권은 `content/wishes.csv`인데 용돈은 부모 화면 설정입니다. **실제 돈이라 PIN 뒤에 두는 편이 맞습니다.**

```jsonc
"allowance": { "on": false, "per": 100, "won": 200, "day": 6, "max": 500 }
```

기본값이면 토요일마다 **최대 1,000원**입니다(주 수입 약 700풀 중 500).

`public_settings()`에 `allowance`를 더했습니다. **화이트리스트 방식은 그대로 유지했습니다** — 전개(`**s`)로 바꾸면 나중에 AI 키가 들어오는 날 바로 새어 나갑니다.

### 17.3 상한이 두 소비처를 갈라놓습니다

풀은 **아이템과 용돈이 같이 씁니다.** 상한이 없으면 아이가 모은 풀을 전부 돈으로 바꿔버려서, 가게에 사고 싶은 게 있어도 못 삽니다.

`max`가 그 장치입니다. 그리고 아이가 100·300·500 중에 **고릅니다** — 자동으로 다 바꾸지 않습니다.

### 17.4 주 1회

`allowanceDay()`가 셋을 다 봅니다 — 켜져 있고, 오늘이 그 요일이고, **오늘 아직 안 바꿨을 때**. 요일이 고정이라 "그날 한 번"이 곧 "주 한 번"입니다.

매일 환전되면 임금이 되고, 임금이 되면 놀이가 노동이 됩니다(원칙 2.5).

### 17.5 기록은 지우지 않습니다

`P.allowance`는 **바꾼 기록**입니다. 소원권과 같은 규칙입니다. 부모 화면에 날짜·금액·합계가 나옵니다.

### 17.6 시뮬레이션 — 5B 완료 시점 (180일)

| | 소원권·용돈 없이 | 5B 전부 |
|---|---|---|
| 별 소비처가 마르는 날 | 140일 | 안 마름 |
| 180일 별 잔고 | 17 (쓸 곳 없음) | 5 |
| **180일 풀 잔고** | **10,309** | **1,845** |
| 180일 용돈 | — | 17,200원 |

두 화폐 모두 180일에 살아 있습니다. **5B의 목표였던 "8주가 지나도 살 게 없다는 상태가 오지 않는다"(성공 기준 2)를 넘겼습니다.**

곁들여 나온 관찰 하나 — 용돈을 켜면 아이템 구매가 느려집니다(90일 기준 27/30 → 24/30). 풀 하나를 두 곳이 나눠 쓰니 당연하고, **어느 쪽을 고를지는 아이가 정합니다.** 그게 이 설계의 의도입니다.

### 17.7 5B 전체 정리

| | 화폐 | 유한한 소비처 | 마르는 날 | 무한한 소비처 |
|---|---|---|---|---|
| ⭐ | 별 | 캐릭터 88 + 배경 83 = 171 | 140일 | 소원권 |
| 🌿 | 풀 | 아이템 48종 14,810 | 180일에 45/48 | 용돈 |

```
node test/t_book.mjs    30개   도감
node test/t_shop.mjs    35개   상점 단계·전용 아이템
node test/t_wish.mjs    33개   배경·소원권
node test/t_allow.mjs   24개   용돈
node sim/60days.mjs 180        보상 균형
```

네 묶음 모두 공통으로 **"이미 받은 것이 줄어드는 경로가 없는가"** 를 봅니다. 판 보상 · 상점 단계 · 소원권 목록 · 용돈 기록. 5B에서 늘어난 것이 전부 그 위험을 갖고 있어서입니다.

**다음은 6A 단답형입니다**(기획서 12.1). 보상이 아니라 입력이라 성격이 다릅니다 — 위험은 *막힌 아이가 누를 것이 없는 상태*입니다.

---

## 18. 학습 진도 관리 검토 (2026-08-19) — 5C 제안

부모 요청: **과목 → 단원 → 문제**로 관리하고, 아이의 학교 진도에 맞춰 미리 세팅하고 싶다.

기획서 16장에 설계를, 기술설계서 5.7에 구현을 적었습니다. 여기는 **지금 코드에서 실제로 걸리는 것**만 적습니다.

### 18.1 지금 코드에서 바로 걸리는 것 두 개

**`sorted(folder.glob('*'))`가 문자열 정렬입니다.**

```
지금:      1-1 · 1-10 · 1-2 · 1-3 · 2-1
자연 정렬:  1-1 · 1-2 · 1-3 · 1-10 · 2-1
```

단원이 열 개를 넘는 순간 진도 순서가 뒤집힙니다. 그리고 순서는 **`engine.js`의 `order`로 이어져 출제 순서까지** 바꿉니다. 나중에 고칠 것이 아니라 처음부터 넣어야 합니다.

**`scan()`이 폴더를 안 내려갑니다.** `glob('*')`에 디렉터리도 걸리는데 `suffix` 검사에서 조용히 빠집니다. 부모가 폴더를 만들면 **아무 일도 안 일어납니다** — 오류도 안 납니다. 지금 이 상태가 제일 나쁩니다.

### 18.2 팩 id를 바꾸면 안 됩니다

`수학/1-1 구구단.csv`의 팩 id를 `수학-1-1-구구단`으로 만들고 싶은 유혹이 있습니다. **하지 마세요.**

문제 키가 `{팩id}:{문제}`라서, id가 바뀌면 **아이가 몇 달 쌓은 진도·마스터·스티커가 통째로 새 문제로 취급됩니다.** 파일을 폴더로 옮기는 것만으로 그렇게 됩니다.

→ id는 **파일명 슬러그만**. 과목은 별도 필드. 다른 폴더에 같은 파일명이 있을 때만 충돌이고, 그건 경고로 처리합니다.

### 18.3 `progress`와 `disabled`를 합치지 마세요

진도를 `disabled.packs`로만 표현하면 서버·엔진 변경이 0이라 매력적입니다. **하지만 안 됩니다.**

`disabled`는 "빼는 목록"이라, **뒤에 새 파일을 넣으면 목록에 없으니 바로 열립니다.** 다음 학기 분량을 미리 넣어두는 것이 이 기능의 존재 이유인데 그게 깨집니다.

`progress`는 "여기까지"라서 뒤에 뭐가 추가돼도 안 열립니다. 둘은 역할이 다르고 **둘 다 필요합니다.**

| | 뜻 | 되돌릴 수 있나 |
|---|---|---|
| `progress` | 학교 진도. 과목마다 "여기까지" | **예** (16.4) |
| `disabled` | "이 문제는 지금 빼자" | 예 (5A) |

### 18.4 진도는 내려도 됩니다 — 별·스티커와 다릅니다

지금까지 "내려가면 안 되는 것"이 셋이었습니다 — 상점 단계(12장), 도감 판(14장), 받은 소원권·용돈(16·17장). **진도는 그 셋과 다릅니다.**

내려가도 **사라지는 것이 없기 때문**입니다. 앞으로 그 문제가 안 나올 뿐, `facts`·별·스티커·해금·상점 단계는 전부 그대로입니다. 5A의 문제 켜고 끄기와 같은 규칙입니다(2.9).

부모 화면에 그 문장을 그대로 띄우세요 — 그게 없으면 부모가 진도를 못 내립니다.

### 18.5 아이 화면에 안 열린 단원을 보여주지 마세요

목장의 회색 실루엣(2.6)을 그대로 따라 하고 싶겠지만, **반대로 해야 합니다.**

| | 회색으로 보여주나 | 누가 여나 |
|---|---|---|
| 다음에 만날 친구 | 예 | **아이** — 별을 모아서 |
| 아직 안 배운 단원 | **아니오** | **학교** |

아이가 통제할 수 없는 것을 눈앞에 두면 목표가 아니라 압박입니다. 2.6이 잘 통한 이유는 그게 아이가 **여는** 것이었기 때문입니다.

### 18.6 별 페이싱을 다시 봐야 합니다

지금 별을 주는 문제가 **75개**입니다. 수학 4단원 + 한국사 4단원이면 **150~200개**가 됩니다. 마스터 보상(문제당 별 1)이 두세 배가 됩니다.

거기에 **단원 완주 보상 별 5**(기획서 8.2의 "팩 하나 전체 마스터")가 단원 단위로 훨씬 자주 터집니다. 지금은 팩이 커서 거의 안 나오는 보상인데, 단원은 15~24문제라 자주 나옵니다.

소원권(8.8)이 무한 소비처라 구조는 버팁니다. 그래도 **`node sim/60days.mjs 180`을 문제 파일을 늘린 뒤 다시 돌려야 합니다.** 시뮬레이션은 `content/problems`를 실제로 읽으므로, 파일만 넣으면 바로 새 숫자가 나옵니다.

한 가지 더 — `TIER_NEED`의 5단계 조건 **65문제**는 지금 75개 기준입니다. 문제가 150개가 되면 5단계가 너무 빨리 열립니다. **조건은 올리면 안 되므로**(원칙 2.1), 6단계를 뒤에 추가하는 방식으로 늘리세요.

### 18.7 작업 크기

| | |
|---|---|
| `packs.py` | 재귀 스캔 · 자연 정렬 · `subject`/`unit`/`order` 필드 |
| `engine.js` | `buildIndex(packs, disabled, progress)` — **세 번째 인자만** |
| `app.js` | 자유 모드 2단계, 새 단원 알림 |
| `parent.html` | 과목별 진도 드롭다운, 단원 목록, "다음 단원 열까요?" 제안 |
| `server.py` | **없음** — 팩 메타에 필드가 늘 뿐 |

`openKeys`·`poolSize`·`pickKey`는 **손대지 않습니다.** 5A에서 `disabled`를 넣을 때와 같습니다(2.10).

### 18.8 순서 제안 — 6A보다 먼저

**5C로 6A(단답형) 앞에 넣기를 권합니다.**

- 지금 실사용 중이고, 문제 파일이 늘어나는 순간 바로 필요합니다
- 6A가 먼저 들어가면 `유형` 열과 폴더 구조를 **같이** 옮겨야 해서 부모가 두 번 고칩니다
- 자연 정렬은 지금 **조용히 틀리고 있는 버그**입니다. 단원을 안 쓰더라도 고쳐야 합니다

```
5C-1  재귀 스캔 + 자연 정렬 + subject/unit 필드      아이 화면 변화 없음
5C-2  progress 필드 + buildIndex 세 번째 인자
5C-3  부모 화면 진도 드롭다운 + 다음 단원 제안
5C-4  아이 화면 — 자유 모드 2단계, 새 단원 알림
```

5C-1은 **아이 화면이 전혀 안 바뀌는** 작업이라 위험이 낮습니다. 5A·6B와 같은 성격입니다.

---

## 19. 재귀 스캔과 자연 정렬 (5C-1, 2026-08-19 완료)

18장의 첫 항목입니다. **아이 화면은 한 줄도 안 바뀌었습니다.**

### 19.1 고친 것

| | |
|---|---|
| `natural(name)` | 숫자 조각을 숫자로 비교. `1-10`이 `1-2` 뒤로 갑니다 |
| `scan()` | 폴더를 **한 단계** 내려갑니다. 폴더 이름이 과목 |
| `read_pack()` | `scan()`에서 떼어냈습니다. `subject`·`unit`·`order` 추가 |
| `server.py` | 팩 메타에 세 필드를 실어 보냅니다. **엔드포인트는 그대로** |
| `parent.html` | 팩 목록을 과목별로 묶어 보여줍니다 (읽기 전용) |

### 19.2 자연 정렬은 화면 순서가 아닙니다

```
지금까지:  1-1 · 1-10 · 1-2 · 1-3 · 2-1
고친 뒤:   1-1 · 1-2 · 1-3 · 1-10 · 2-1
```

이 순서가 `order`가 되고, `order`는 `engine.js`의 **"앞에서 3개씩 열기"**(7.2)로 이어집니다. 정렬이 틀리면 **엉뚱한 문제가 먼저 열립니다.** 단원을 안 쓰더라도 고쳐야 했던 이유입니다.

### 19.3 팩 id는 파일명 슬러그 그대로입니다

`수학/1-1 구구단.csv`의 id는 `1-1-구구단`입니다. **과목이 안 들어갑니다.**

id가 바뀌면 문제 키(`{팩id}:{문제}`)가 바뀌고, 아이가 쌓은 진도·마스터·스티커가 통째로 새 문제가 됩니다. 그래서 **파일을 폴더로 옮기는 것만으로는 아무것도 안 잃습니다** — 부모가 지금 구조를 안심하고 정리할 수 있어야 합니다.

`t_units.mjs`가 이걸 직접 봅니다 — 같은 파일을 폴더 밖과 폴더 안에서 스캔해 id가 같은지.

**대신 이름 충돌이 생깁니다.** 다른 폴더에 같은 파일명이 있으면 먼저 온 것만 싣고 경고를 답니다. 실제로는 `1-1 구구단.csv` 처럼 쓰므로 드뭅니다.

### 19.4 기존 파일은 그대로

폴더 밖의 파일은 `subject: null`, `unit: null`이고 **진도 관리 대상이 아닙니다.** 항상 열립니다.

저장소의 파일 3개(`과학-식물`·`나눗셈`·`한국사-옛날사람들`)는 한 글자도 안 고쳤고 그대로 읽힙니다. 옮길지 말지는 부모가 정합니다.

### 19.5 단원 번호

파일명이 숫자로 시작하면 그 부분이 단원입니다 — `1-1`, `2`, `3.1`. 없으면 `null`이고 진도 관리 대상이 아닙니다.

진도 비교는 **`unit` 문자열이 아니라 `order`로** 합니다(기술설계서 5.7). 그래서 부모가 `1-1`을 쓰든 `01`을 쓰든 순서만 맞으면 됩니다.

### 19.6 검증

```
node test/t_units.mjs     # 24개
```

파이썬으로 임시 폴더에 가짜 문제 파일을 만들어 `packs.scan()`을 돌립니다. 자연 정렬·과목 추출·단원 추출·이름 충돌·두 겹 폴더 무시, 그리고 **id가 안 바뀌는 것**.

마지막 두 검사는 **저장소의 실제 파일**을 읽습니다 — 3개가 그대로 읽히고 전부 과목 없음인지.

### 19.7 다음 (5C-2)

`progress` 필드와 `buildIndex`의 세 번째 인자입니다. 여기서부터 **아이 화면이 바뀝니다** — 안 열린 단원의 문제가 안 나오기 시작합니다.

넣기 전에 `node sim/60days.mjs 180`을 돌려두세요. 문제 수가 바뀌면 별 페이싱이 따라 바뀝니다(18.6).

---

## 20. 진도 필터 (5C-2, 2026-08-19 완료)

`progress` 필드와 `buildIndex`의 세 번째 인자입니다.

### 20.1 무엇이 생겼나

```js
buildIndex(packs, disabled, progress)     // 아이 화면
buildIndexAll(packs)                      // 부모 화면 — 진도·제외를 전부 무시
openPacks(packs, progress)                // 지금 열려 있는 팩 id들
```

```jsonc
"progress": { "수학": "1-3", "한국사": "1-1" }
```

`openKeys`·`poolSize`·`pickKey`는 **손대지 않았습니다.** 5A에서 `disabled`를 넣을 때와 같습니다(2.10) — 엔진 입장에서는 "원래 그만큼인 팩"입니다.

### 20.2 열리는 규칙

| 팩 | 언제 열리나 |
|---|---|
| 과목이 없음 (폴더 밖) | **늘** |
| 과목은 있는데 단원 번호가 없음 | **늘** — 진도 관리 대상이 아닙니다 |
| 과목·단원이 있고 진도를 안 정함 | **첫 단원만** |
| 과목·단원이 있고 진도가 있음 | 그 단원까지 |
| 내장 구구단 | **늘** — 과목이 없습니다 |

비교는 **자연 정렬**입니다. `1-3`까지 열었을 때 `1-10`은 안 열립니다. `engine.js`의 `natCmp`가 `packs.py`의 `natural()`과 같은 규칙입니다 — **두 곳에 같은 규칙이 있으니 한쪽만 고치지 마세요.**

### 20.3 부모 화면은 전부 봅니다

`parent.html`이 `buildIndexAll()`을 씁니다. **부모는 아직 안 연 단원도 봐야 진도를 정할 수 있습니다.**

전에는 `buildIndex(loaded)`였는데, 진도 기본값이 "첫 단원만"이라 그대로 뒀으면 부모 화면에서 안 연 단원이 **조용히 사라졌을** 것입니다.

### 20.4 무한 재귀를 만들 뻔했습니다

"색인이 비면 전부 무시하고 다시 짓는다"는 기존 보호 장치가 있습니다(2.10). 여기에 진도를 얹으면서 조건을 `packs.length`로 잡았더니, `buildIndexAll`이 빈 결과를 내면 **자기를 다시 부르며 무한히 돕니다.**

```js
const filtered = offQ.size || offP.size || open.size < packs.length;
if (!Object.keys(index).length && filtered) return buildIndexAll(packs);
```

`buildIndexAll`은 전부 열므로 `open.size === packs.length`가 되어 `filtered`가 거짓입니다. **무언가를 실제로 걸렀을 때만** 되돌아갑니다.

### 20.5 지금 아이 화면은 안 바뀝니다

저장소의 팩 3개가 전부 **폴더 밖**이라 `subject`가 없습니다. 늘 열립니다. 실사용 프로필도 그대로입니다.

`progress` 필드는 아이가 다음에 들어올 때 `{}`로 만들어집니다(`enter()`의 가드).

### 20.6 ~~5C-3 없이 폴더를 만들지 마세요~~ — 풀렸습니다

5C-3(21장)에서 부모 화면 진도 드롭다운이 생겼습니다. **이제 폴더를 만들어도 됩니다.**

### 20.7 검증

```
node test/t_units.mjs     # 40개 (24 → 40)
```

파이썬 스캔 검사에 **진도 검사 16개**가 붙었습니다. 특히:

- 진도를 안 정하면 첫 단원만
- `1-3`까지 열었을 때 `1-10`이 안 열리는지 (자연 정렬)
- 제외와 진도가 따로 노는지
- **무한히 돌지 않는지**

---

## 21. 부모 화면 진도 (5C-3, 2026-08-19 완료)

이제 **폴더를 만들어도 됩니다.** 20.6의 경고가 풀렸습니다.

### 21.1 화면

```
학습 진도
  수학          여기까지 열기 [1-3 나눗셈 ▼]  [저장]
   ✓ 1-1  구구단              문제 15개 · 완전히 외운 것 12
   ✓ 1-2  2자리 곱셈          문제 20개 · 완전히 외운 것 5
   ✓ 1-3  나눗셈              문제 24개 · 완전히 외운 것 0
   ┈┈┈┈ 여기부터는 아직 안 열렸습니다
   ·  1-4  나머지가 있는 나눗셈  문제 18개
```

체크박스 스무 개가 드롭다운 하나가 됐습니다. **뒤에 새 파일을 넣어도 안 열립니다.**

과목 폴더가 하나도 없으면 만드는 법을 안내합니다 — 지금 저장소가 그 상태입니다.

### 21.2 열림 판정을 두 곳에 두지 않았습니다

부모 화면이 `engine.js`의 **`openPacks()`를 그대로 가져다 씁니다.** 부모 화면에 "이 단원이 열렸나"를 다시 구현하면, 아이 화면과 어긋나는 날이 반드시 옵니다.

`t_units.mjs`가 그것까지 봅니다 — `parent.html`이 `openPacks`를 import하고 실제로 쓰는지.

### 21.3 저장은 `disabled`와 똑같은 방식입니다

```js
const fresh = await api(`/api/profile/${id}`);   // 아이가 지금 놀고 있을 수 있다
fresh.progress = { ...(fresh.progress || {}), [subject]: unit };
```

방금 상태를 다시 받아 `progress`만 얹습니다(2.11). **서버 변경은 0입니다.**

### 21.4 내릴 때 안내가 뜹니다

드롭다운을 낮추면 그 자리에서 뜹니다.

> *1-2까지로 내리면 그 뒤 단원이 앞으로 안 나옵니다. 아이가 쌓은 진도·별·스티커는 그대로 남습니다.*

**이 문장이 없으면 부모가 무서워서 진도를 못 내립니다.** 아이가 어려워할 때 한 단원 접어두는 것이 이 기능의 절반입니다(기획서 16.4).

### 21.5 다음 단원을 제안합니다 — 열지는 않습니다

열린 마지막 단원을 **전부 마스터**했으면 버튼이 하나 뜹니다.

> *나눗셈을 다 외웠어요.* `[1-4 나머지가 있는 나눗셈 열기]`

**자동으로 열지 않습니다.** 게임이 학교보다 앞서가면 아이가 안 배운 것을 풀게 됩니다(기획서 16.7). 누르는 것은 부모입니다.

### 21.6 검증

```
node test/t_units.mjs     # 46개 (40 → 46)
```

붙은 6개는 **부모 화면이 조용히 죽는 것**을 막습니다. 5A에서 `isRewardable` import 하나가 없어서 부모 화면 스크립트가 통째로 실행되지 않은 적이 있습니다(2.12). ES 모듈은 링크 단계에서 실패하고 **에러도 화면에 안 보입니다.**

`parent.html`의 import 목록을 정규식으로 뽑아 `engine.js`의 export와 대조합니다. 이름을 바꾸면 그 자리에서 실패합니다.

### 21.7 남은 것 (5C-4)

아이 화면입니다.

- 자유 모드가 과목 → 단원 두 단계로 (기획서 16.8)
- **새 단원이 열리면 홈에 알림** (16.6) — 부모가 진도를 챙기는 일이 아이에게 선물이 되는 지점입니다

지금은 자유 모드에 단원이 평평하게 나열됩니다. 과목이 하나뿐이면 그대로 괜찮습니다.

---

## 22. 아이 화면 — 자유 모드와 새 단원 (5C-4, 2026-08-19) — 5C 끝

### 22.1 자유 모드가 두 단계가 됩니다

```
무엇을 풀까?            수학
  다 섞어서               수학 다 섞어서
  수학           →        1-1 구구단
  한국사                  1-2 2자리 곱셈
  과학-식물               1-3 나눗셈
                          다른 것 고르기
```

**과목이 하나도 없으면 지금 그대로** 한 단계입니다. 폴더를 안 만든 집에서는 화면이 안 바뀝니다.

과목 없는 팩(폴더 밖)은 1단계에 그대로 나열됩니다. 과목과 섞여 있어도 아이 눈에는 그냥 "고를 수 있는 것"입니다.

### 22.2 엔진에 과목 필터가 생겼습니다

```js
makeQuestion(index, facts, recent, today, want)
// want:  null(전부)  ·  { pack: 'm2' }  ·  { subject: '수학' }  ·  'm2'(문자열 = 팩)
```

`openKeys`·`poolSize`·`pickKey` 세 곳에 흩어져 있던 `entry.packId !== packId` 비교를 **`inPick(entry, pick)` 한 곳으로** 모았습니다. 필터를 또 늘려도 고칠 곳이 하나입니다.

문자열을 넘기면 팩 하나로 봅니다 — 예전 호출부와 시뮬레이션이 그대로 돕니다.

**`MIN_POOL = 8` 보정이 과목 단위에서도 그대로 돕니다**(2.7). 단원 하나를 골라도 같은 문제가 반복되지 않습니다. 테스트에서 확인합니다.

### 22.3 안 열린 단원은 어디에도 안 보입니다

`packList()`가 색인에서 만들어지고, 색인에는 열린 팩만 있습니다. **회색으로 보여주는 코드가 아예 없습니다.**

목장의 다음 친구는 아이가 별을 모아 **스스로 여는 것**이라 목표가 됩니다. 단원은 **학교가 여는 것**이라 눈앞에 두면 압박입니다(기획서 16.5). 테스트가 `app.js`에 그런 코드가 생기지 않는지 봅니다.

### 22.4 새 단원 알림

부모가 진도를 올리면 아이 홈에 노란 버튼이 뜹니다.

> **새로운 문제가 왔어! 1-4 나머지가 있는 나눗셈**

`P.seenPacks`에 지금까지 본 팩 id를 두고, 들어올 때 비교합니다. **처음 들어온 아이에게는 안 알립니다** — 그건 새로 열린 게 아니라 원래 있던 것입니다.

알림 문구는 `P.settings.newUnits`에 담아 둡니다(`newTier`와 같은 방식). 문제를 풀러 가거나 "무엇을 풀까"에 들어가면 지워집니다. **아이가 못 보고 지나칠 일이 없습니다.**

> 이것이 기획서 16.6의 지점입니다 — 학교에서 새 단원을 배운 날 부모가 열어주면,
> **부모가 진도를 챙기는 일이 아이에게는 선물이 됩니다.**

### 22.5 검증

```
node test/t_units.mjs     # 59개 (46 → 59)
```

붙은 13개는 자유 모드 필터와 아이 화면입니다. 특히:

- 과목을 골랐을 때 그 과목만 나오는지
- **안 열린 단원이 `packList`에 없는지**
- 과목 단위에서도 `MIN_POOL` 보정이 도는지 (200번 뽑아 4종 이상)
- `app.js`에 안 열린 단원을 회색으로 그리는 코드가 없는지
- 처음 들어온 아이에게 알림이 안 뜨는지

`node sim/60days.mjs`도 그대로 돕니다 — `makeQuestion`의 다섯 번째 인자가 문자열/객체를 다 받기 때문입니다.

### 22.6 5C 정리

```
5C-1  재귀 스캔 · 자연 정렬 · subject/unit/order      packs.py
5C-2  progress · buildIndex 세 번째 인자 · openPacks   engine.js
5C-3  부모 화면 진도 드롭다운 · 다음 단원 제안          parent.html
5C-4  자유 모드 2단계 · 새 단원 알림                  app.js
```

**서버는 팩 메타에 필드 세 개가 늘어난 것이 전부입니다.** 엔드포인트는 하나도 안 늘었습니다.

폴더를 안 만든 집에서는 **아무것도 안 바뀝니다.** 저장소의 팩 3개가 그 상태입니다.

### 22.7 폴더를 만드시면 확인할 것

- 부모 화면 `학습 진도`에서 각 과목의 진도를 한 번 맞춰주세요. **처음엔 첫 단원만 열립니다**
- 별을 주는 문제가 늘어납니다. `node sim/60days.mjs 180`을 다시 돌려보세요(18.6)
- 상점 5단계 조건 **65문제**가 지금 75개 기준입니다. 문제가 150개가 되면 너무 빨리 열립니다. 조건은 올리면 안 되므로 **6단계를 뒤에 추가**하세요
- 글자 정답 단원은 **서로 다른 정답이 4개 이상** 필요합니다(2.2). 한국사처럼 답이 시대 이름 두세 개로만 갈리면 문제가 통째로 빠집니다

---

## 23. 기록 내보내기와 PWA (2026-08-19)

친구들에게 정적 배포(호스팅)를 하기 전에 먼저 해둔 두 가지입니다. **친구들 얘기가 없어도 해야 할 일**이었습니다 — 지금 방식에서도 아이 기록이 더 안전해집니다.

### 23.1 왜 이것부터인가

정적 배포로 가면 진행이 **브라우저에** 저장됩니다. 그런데 **iOS 사파리는 7일간 방문이 없으면 localStorage를 지웁니다**(ITP). 아이가 일주일 안 하면 몇 주 모은 별·아이템·도감이 통째로 날아갑니다.

지금은 서버가 원자적 저장 + 30일 백업을 해서 그 위험이 없습니다. 배포 전에 두 가지가 필요합니다.

| | 무엇을 막나 |
|---|---|
| **홈 화면에 추가 (PWA)** | 사파리가 지우는 대상에서 빠집니다 |
| **내보내기·되돌리기** | 그래도 날아갔을 때 되돌릴 것이 있습니다 |

### 23.2 내보내기 · 되돌리기 (`/parent`)

`지금 기록 내보내기` → `양이랑구구단-{아이}-{날짜}.json` 파일이 받아집니다. 파일이 안 되는 환경을 위해 **글자로 복사하기**도 접어 두었습니다(v1 이관과 같은 방식).

되돌리기는 **파일을 고르거나 붙여넣으면 지금 기록과 나란히** 보여줍니다.

```
              지금    되돌리면
별             11        3      ← 줄어드는 값은 눈에 띄게
완전히 외운 것   18        5
```

**되돌리기는 덮어쓰기입니다.** 이 프로젝트에서 가장 위험한 버튼이라 앞을 여러 겹으로 막았습니다.

- `readBackup()`이 `wallet`·`facts`·`totals`가 다 있는지 봅니다. 엉뚱한 파일로 덮어쓰면 되돌릴 수 없습니다
- 다른 아이 기록이면 이름을 짚어 경고합니다
- `confirm`으로 한 번 더 묻습니다
- *"되돌리기 전에 지금 기록 내보내기를 한 번 눌러두세요"* 를 그 자리에 적어둡니다
- 되돌릴 때 `id`를 **지금 고른 아이로 맞춥니다.** 파일 이름과 `id`가 어긋나면 다음 저장이 엉뚱한 데로 갑니다

내보내기는 **방금 상태를 다시 받아서** 씁니다. 아이가 지금 놀고 있을 수 있습니다.

**아이 화면에는 넣지 않았습니다.** 아이 화면에서 부모 화면으로 가는 경로를 만들지 않습니다(원칙 2.7).

### 23.3 PWA

```
web/manifest.json          standalone · 세로 · 목장 초록
web/icons/icon-180.png     apple-touch-icon — iOS는 이것만 봅니다
web/icons/icon-192.png
web/icons/icon-512.png     maskable로도 씁니다
sim/make-icons.py          아이콘 굽기
```

**아이콘은 파이썬 표준 라이브러리로 굽습니다.** `zlib` + `struct`로 PNG를 직접 씁니다 — 외부 패키지 없음이 이 프로젝트의 조건이고(기술설계서 1장), SVG를 래스터화할 방법이 없어서 양 얼굴을 픽셀로 직접 그렸습니다. 3×3으로 크게 그린 뒤 줄여서 가장자리를 부드럽게 합니다.

도형 좌표가 전부 **0~1 비율**이라 크기를 더 늘려도 그대로 나옵니다.

```
python3 sim/make-icons.py
```

### 23.4 홈 화면 안내

iOS이고, 아직 안 넣었고, 닫은 적이 없을 때만 홈 화면 아래에 조용히 한 줄 뜹니다.

> 홈 화면에 넣어두면 앱처럼 바로 열려.
> 위쪽 **공유 버튼**을 누르고 **홈 화면에 추가**를 고르면 돼.

**아이에게 이유를 말하지 않습니다.** "안 하면 기록이 날아간다"는 부모가 알 일입니다. 아이 화면에 겁을 주는 문장을 두지 않습니다. 테스트가 그 문구가 안 들어가는지 봅니다.

iOS는 설치 버튼을 띄울 방법이 없어서(`beforeinstallprompt` 없음) 글로 알려주는 수밖에 없습니다.

### 23.5 곁들여 고친 것

`send_bytes()`가 **모든** 응답에 `; charset=utf-8`을 붙이고 있었습니다. PNG에도 붙었습니다. 지금까지는 이미지가 없어서 안 드러났습니다.

```python
texty = any(ctype.startswith(k) for k in self.TEXTY)
```

### 23.6 검증

```
node test/t_backup.mjs     # 34개
```

`summarize()`·`readBackup()`을 `parent.html`에서 떼어 검사하고, 화면이 지키는 것(확인 묻기·덮어쓰기 경고·id 맞추기·아이 화면에 없음)을 정규식으로 봅니다.

아이콘은 **진짜 PNG이고 크기가 맞는지** 바이트를 읽어 확인하고, `make-icons.py`가 다시 돌아가는지도 봅니다.

### 23.7 남은 것 — 정적 배포

이제 배포로 갈 수 있습니다. 남은 작업입니다.

```
1. 빌드 스크립트   content/ → web/content.json  (packs.scan() 결과를 덤프)
2. 저장 계층 교체  fetch(/api/profile) → localStorage
3. GitHub Actions  push하면 자동 빌드·배포
```

배포 전에 확인할 것 — 저장소가 **공개**가 됩니다. `messages.csv`는 이름을 `{이름}` 치환자로 쓰고 있어 지금은 안전하지만, 집안 농담을 넣으면 그것도 공개됩니다. 문제 파일의 출처도 한 번 보세요.

---

## 24. 문제 팩 재편 — 수학과 한국사 (2026-08-20)

`docs/category_plan.md`의 구성을 실제로 만들었습니다. **기존 팩 3개는 지웠습니다.**

### 24.1 계획서가 5C보다 먼저 쓰였습니다

`category_plan.md`는 파일명에 과목을 넣는 방식(`수학-1-3-나눗셈.csv`)이었습니다. 그대로 두면 5C에서 `subject: null`, `unit: null`이 되어 **진도 관리 대상이 아니고 늘 열립니다.** 계획서 11장의 "붙기 전에 필요한 코드 작업" 네 가지는 이미 다 되어 있었는데 **다른 방식으로** 되어 있었습니다.

→ 계획서를 폴더 방식으로 다시 썼습니다. 학기는 단원 번호가 그대로 담습니다 — `1-3` = 1학기 3단원.

### 24.2 계획서의 한국사 설계가 스스로 모순이었습니다

계획서는 시대별로 파일을 나누면서 묶음을 `시대`로 두라고 했습니다. 그런데 한 파일에 시대가 둘뿐이라 **정답이 2종**이고, 4종 미만이면 문제가 통째로 빠집니다(2.2). 계획서 자신이 112줄에 그 규칙을 적어놓고 74줄의 분할이 그걸 위반합니다.

실제로 5C-1 확인 때 그 실패를 재현했습니다 — 한국사 4문제가 전부 빠졌습니다.

**두 가지로 풀었습니다.**

1. **묶음을 답의 종류로** — `시대`가 아니라 `도구`·`살던 곳`·`유물`·`인물`·`싸움`. 한 묶음에 답이 4~6종 모입니다
2. **답이 적을 수밖에 없는 질문은 오답을 직접** — 시대 질문에 같은 파일에 없는 시대(청동기·철기)를 오답으로 씁니다. 오히려 자연스러운 보기가 됩니다

### 24.3 수학은 손으로 쓰지 않았습니다

```
python3 sim/seed-math.py [--force]
python3 sim/seed-history.py [--force]
```

257문제를 손으로 적으면 반드시 계산 실수가 나고, **아이가 맞게 답하고 틀린 것으로 처리되는 것이 이 게임에서 가장 나쁜 결과**입니다.

`t_units.mjs`가 곱셈·나눗셈·몫·나머지를 **독립으로 다시 계산해** 확인합니다. 지금 0개 오류입니다.

두 스크립트 모두 **이미 있는 파일은 건너뜁니다.** 부모가 손으로 고친 것을 날리면 안 됩니다.

### 24.4 만든 것

| | 단원 | 문제 |
|---|---|---|
| 수학 | 1-3 ~ 2-5, 9개 | 257 |
| 한국사 | 1-1 ~ 2-3, 8개 | 165 |
| | | **422** (+ 내장 구구단 39) |

**경고 0개** — 빠진 문제가 하나도 없습니다.

### 24.5 구구단이 수학 과목에 들어갑니다

`수학` 폴더가 있으면 내장 구구단의 `subject`가 `수학`이 됩니다. 자유 모드에서 구구단만 따로 떨어져 있으면 아이 눈에 이상합니다.

**단원 번호가 없으므로 진도와 상관없이 늘 열려 있습니다** — `openPacks`의 "과목은 있는데 단원 번호가 없으면 늘" 규칙이 그대로 적용됩니다. 새 규칙을 만들지 않았습니다.

수학 폴더가 없는 집에서는 예전처럼 과목 없이 남습니다.

### 24.6 지운 팩 — 무엇이 남고 무엇이 사라지나

`과학-식물` · `나눗셈` · `한국사-옛날사람들`을 지웠습니다. 아이가 그 팩에서 쌓은 것 중에서:

| | |
|---|---|
| **남는 것** | 별 · 풀 · `totals.mastered` · **도감 스티커** · 해금한 친구 · 상점 단계 |
| 사라지는 것 | 그 팩 문제들의 마스터리(`m`) — 앞으로 안 나오므로 의미가 없어집니다 |

도감 스티커 9장이 그대로 보입니다. `stickerLabel()`이 색인에 없는 키도 읽기 때문입니다(14.4).

```
나눗셈:9÷3                        → 9÷3
한국사-옛날사람들:구석기사람들은…   → 구석기사람들은…
```

**이것이 원칙 2.9가 화면에서 지켜지는 자리입니다.** 문제를 바꿔도 아이가 받은 것은 사라지지 않습니다.

### 24.7 별 페이싱 — 확인했습니다

시뮬레이션에 버그가 있었습니다. **`packs.scan()` 결과를 덤프할 때 `subject`·`unit`을 안 실어서** 진도 필터가 아예 안 걸리고 있었습니다(5B 때 쓴 코드입니다). 고치고 `--all` 모드를 더했습니다.

| 120일 | 진도 기본값 (65문제) | 모든 단원 열림 (437문제) |
|---|---|---|
| 마스터 | 65 | 140 |
| 별 번 것 | 146 | 224 |
| **별 잔고** | **15** | **5** |
| 소원권 | 0장 | 4장 |
| 아이템 | 27/30 | 25/30 |

**어느 쪽에서도 별이 안 넘칩니다.** 소원권이 받아냅니다.

18.6에서 걱정했던 "문제가 150개가 되면 상점 5단계가 너무 빨리 열린다"는 실제로 재보니 **문제가 아니었습니다.** 모든 단원을 열어도 60~70일쯤 열리고 120일에 아직 5개가 남습니다. **6단계를 급히 만들 필요가 없습니다.**

### 24.8 검증

```
node test/t_units.mjs     # 68개 (59 → 68)
```

붙은 9개는 **저장소의 진짜 문제 파일**을 봅니다 — 수학 9단원·한국사 8단원·전부 단원 번호가 있는지·경고 0인지·순서가 맞는지, 그리고 **수학 계산이 전부 맞는지**.

문제 파일을 고쳤을 때 이 검사가 실패하면, 아이에게 나가기 전에 잡힙니다.

---

## 25. 정적 배포 (GitHub Pages, 2026-08-20)

친구들이 링크만 열면 할 수 있게 합니다. **집 서버는 지금 그대로 돕니다.**

### 25.1 서버가 하던 일 중 하나만 옮기면 됩니다

| 서버가 하던 일 | 정적에서 |
|---|---|
| `/api/bootstrap`·`/api/pack/{id}` | 빌드 때 `content.json`으로 미리 구움 |
| `/api/parent/*` | 같은 파일 + `localStorage` |
| **`/api/profile/{id}`** | **`localStorage`** ← 이것만 진짜 옮겨야 했습니다 |

### 25.2 `web/store.js` — 한 코드가 두 곳에서

```js
export const STATIC = !!globalThis.window?.GUGUDAN_STATIC;
```

빌드가 `dist/index.html`에 `window.GUGUDAN_STATIC = true`를 심습니다. **`web/`에는 그 줄이 없으므로 집 서버는 서버 모드**입니다. 테스트가 그것까지 봅니다.

`app.js`와 `parent.html`에 **`/api/` 문자열이 하나도 안 남았습니다.** 두 화면이 저장소를 직접 부르면 한쪽만 고치는 날이 오고, 그날 친구들 화면이 조용히 멈춥니다.

### 25.3 새 프로필의 모양은 `server.py`가 정합니다

빌드가 `server.new_profile()`을 그대로 불러 `content.json`에 넣습니다. 정적 모드는 그것을 복사해 씁니다.

**프로필 모양을 JS에 다시 적지 않았습니다.** 두 곳에 두면 반드시 어긋나고, 어긋나면 친구 아이 프로필에 필드가 빠진 채로 만들어집니다.

### 25.4 GitHub Pages는 도메인 뿌리가 아닙니다

`https://아이디.github.io/저장소/` 아래에 올라갑니다. `/app.js` 같은 절대 경로는 **전부 깨집니다.**

빌드가 `dist/`에서만 상대 경로로 바꿉니다 — `href="/…"` → `"./…"`, `from '/…'` → `'./…'`, manifest의 `start_url`·아이콘도. `web/`은 안 건드립니다.

- `.nojekyll` — `_`로 시작하는 경로를 Jekyll이 먹지 않게
- `404.html` — **자산을 하나도 안 부르는 한 장짜리**. 깊은 경로에서 뜨면 상대 경로가 어긋나기 때문입니다
- 부모 화면은 `parent.html`입니다. 정적에는 `/parent` 재작성이 없습니다

### 25.5 곁들여 잡은 것 — 보기 순서가 영영 고정될 뻔했습니다

파일에서 온 문제의 보기는 `packs.py`가 **빌드 때 한 번** 섞습니다. 서버 모드에서는 요청마다 다시 섞이지만, **정적에서는 파일이 한 번 구워지므로 영영 같은 자리**입니다. 아이가 답을 외우는 게 아니라 **자리를 외웁니다.**

→ `makeQuestion()`이 낼 때마다 섞습니다. 서버 모드에서도 좋아집니다 — 지금까지는 한 세션 안에서 자리가 고정이었습니다.

### 25.6 배포

```
python3 sim/build-static.py     # dist/ 를 만든다
```

`.github/workflows/pages.yml`이 `main`에 푸시할 때 돕니다. **검사를 먼저 돌리고 실패하면 배포하지 않습니다** — 문제 파일이 깨졌으면 아이에게 나가기 전에 잡는 게 낫습니다.

처음 한 번 저장소 **Settings → Pages → Source를 "GitHub Actions"** 로 바꿔야 합니다.

`dist/`는 `.gitignore`에 넣었습니다. 산출물을 커밋하지 않습니다.

### 25.7 정적 모드에서 달라지는 것 — 부모에게 알려야 합니다

| | 집 서버 | 정적 (친구들) |
|---|---|---|
| 진행 저장 | PC의 JSON + 30일 백업 | **브라우저** |
| 문제 추가 | 폴더에 파일을 넣으면 끝 | **git push가 필요** |
| 프로필 | 여러 아이가 한 목록에 | 기기마다 따로 |
| 데이터 주인 | 우리 집 PC | **각자 기기** |

**문제 추가가 정적의 진짜 대가입니다.** 성공 기준 3번("부모가 개발자 도움 없이 문제를 추가할 수 있다")이 친구 집에서는 깨집니다. 사실상 문제는 우리가 관리하게 됩니다.

**데이터가 각자 기기에 남는 것은 오히려 좋습니다.** 남의 집 아이 학습 기록을 우리가 보관하지 않고, 기획서 10장의 "계정·로그인·클라우드 동기화 없음"도 지켜집니다.

### 25.8 공용 서버는 여전히 안 됩니다

파이썬 서버를 인터넷에 올리는 방법은 **하지 마세요.**

```python
def list_profiles(): ...        # 모든 아이 이름이 내려간다
def get_profile(self, pid): ... # 인증이 없다
```

친구가 다른 아이 이름을 보고 그 프로필을 열어 진행을 바꿀 수 있습니다. 제대로 하려면 계정이 필요하고, 그건 기획서 10장과 정면으로 부딪힙니다.

### 25.9 검증

```
node test/t_static.mjs     # 53개
```

빌드가 도는지, 절대 경로가 안 남았는지, `content.json`에 다 실렸는지, 그리고 **정적 모드를 node 안에서 흉내내 실제 왕복**을 시킵니다 — 프로필 만들기·저장·불러오기·목록, 부모 화면 PIN과 설정까지.

마지막 두 검사가 **두 모드가 갈라지지 않았는지**를 봅니다: `app.js`와 `parent.html`에 `/api/`가 남아 있으면 실패합니다.

---

## 26. 첫 선반이 비어 있었습니다 (2026-08-24)

아이가 게임은 아주 좋아하는데 **"살 액세서리가 부족하다"** 고 했습니다. v1보다 줄었다는 겁니다.

### 26.1 아이템은 하나도 안 없어졌습니다

먼저 확인한 것은 *뭐가 사라졌나*였는데, 답은 **아무것도**였습니다. v1의 13종은 id·가격 그대로 남아 있고 5B에서 30종으로 늘렸습니다. 줄어든 것은 **한 번에 보이는 수**입니다.

| | v1 | 5B 이후 (1단계) |
|---|---|---|
| 모자 | 7 | 3 |
| 목도리 | 6 | 3 |
| 소품 | — | **0 (탭 자체가 안 보임)** |
| 합 | **13** | **6** |

실제 프로필(`흰양이`)을 열어보니 그 6종 중 **5종을 이미 샀고**(`leaf, straw, ribbon, sblue, syel`), 남은 것은 `sred` 하나였습니다. 마스터 9개 — 2단계(10개)에 **한 문제 모자란** 상태였습니다.

### 26.2 단계 개방이 만든 것은 목표가 아니라 품절이었습니다

기획서 8.3의 논리는 옳습니다. 전 상품을 처음부터 보여주면 목표가 흐려집니다. 하지만 **선반이 비면 그건 목표가 아니라 품절입니다.** 아이 입장에서 "10문제 더 외우면 새 게 들어와"는 *지금 살 게 있을 때* 기다릴 만한 말이고, 살 게 없으면 그냥 닫힌 가게입니다.

소품이 특히 나빴습니다. 탭이 통째로 없어서, 5B에서 슬롯을 하나 늘렸다는 사실 자체가 아이에게 도달하지 않았습니다.

**규칙**: 어느 단계에서도 살 것이 남아 있어야 합니다. 1단계는 v1의 13종이 하한선입니다.

### 26.3 한 것 — 48종

18종을 새로 그리고, 소품 2종(`pballoon`·`pbook`)의 단계를 2 → 1로 **내렸습니다**.

| 단계 | 새로 그린 것 |
|---|---|
| 1 | 물방울 두건 · 도토리 모자 · 초록 목도리 · 물방울 목도리 · 들꽃 다발 |
| 2 | 요리사 모자 · 물안경 · 꽃 목걸이 · 진주 목걸이 · 코코아 · 우산 |
| 3 | 마법사 모자 · 빨간 망토 · 사진기 |
| 4 | 헤드폰 · 작은 기타 |
| 5 | 무지개 머리띠 · 생일 케이크 |

| | 이전 | 지금 |
|---|---|---|
| 아이템 | 30종 | **48종** |
| 총액 | 9,450풀 | **14,810풀** |
| 1단계 선반 | 6종 | **13종** (모자 5 · 목도리 5 · 소품 3) |
| 공용 누적 | 2/9/9/9/9 | **13/26/29/31/33** |

전용 아이템은 **15종 그대로**입니다. 늘린 것은 전부 공용입니다 — 아이가 말한 "캐릭터별로 살 수 있는 것"은 그 캐릭터에게 *입힐 수 있는 것*의 수이고, 공용을 늘리는 쪽이 캐릭터 다섯 모두에게 동시에 닿습니다. 전용을 캐릭터당 6종으로 늘리면 SVG가 30개 더 필요한데, 그러면 아직 못 만난 친구의 물건만 잔뜩 늘어납니다(8.7).

**값과 단계는 하나도 안 올렸습니다.** 내린 것만 있습니다(원칙 2.1).

### 26.4 균형

```
node sim/60days.mjs 180
```

| 일 | 산 것 | 풀 잔고 |
|---|---|---|
| 14 | 11/48 | 37 |
| 60 | 27/48 | 145 |
| 180 | 45/48 | 938 |

이전에는 180일에 30/30 — 그때부터 풀이 벌기만 하고 쓸 데가 없었습니다(구현-현황 7장의 오래된 문제). 이제 180일에도 셋이 남습니다. 풀 잔고가 늘 100 근처인 것은 **살 게 계속 있다**는 뜻이고, 그게 목표한 상태입니다.

### 26.5 검증

```
node test/t_shop.mjs    # 35개 (31 → 35)
```

네 개를 새로 넣었습니다.

- `1단계 N종 — v1의 13종 이상` — 이번 사고를 다시 못 내게 하는 검사입니다
- `1단계에 세 슬롯이 다 있다` — 탭이 통째로 사라지는 것을 잡습니다
- `단계마다 공용이 늘어난다` — 어느 단계가 0종이면 그 단계는 보상이 아닙니다
- `나간 아이템의 값·단계가 오르지 않음` — v1·5B에 나간 30종의 값과 단계를 표로 박아뒀습니다. 값이 오르면 그걸 모으던 아이에게는 손해입니다(원칙 2.1). 이 표를 고쳐야 통과한다면 고치기 전에 왜인지 보세요

`48종이 전부 그려짐`은 5B에서 만든 검사가 그대로 잡아줍니다 — 정의만 있고 그림이 없으면 화면에는 아무 일도 안 일어나면서 풀만 빠져나갑니다(15장).

### 26.6 눈으로 볼 것

`translate(a) scale(s) translate(-b)`는 테스트로 NaN만 잡지 모양은 못 봅니다. 특히 이 넷:

- **빨간 망토**(`ncape`) — 목 앵커에 붙는 것 중 처음으로 몸을 덮습니다. 배율 1.3인 카피바라에서 다리를 가리지 않는지
- **작은 기타**(`pguitar`) — 소품 중 가장 높이 올라갑니다(y 92). 고양이 꼬리와 겹치는지
- **헤드폰**(`hphone`) — 귀 옆으로 컵이 나갑니다. 토끼 귀, 코알라 귀와 부딪히는지
- **우산**(`pumbrella`) — 가장 넓습니다(x 114~182). 배율 1.05에서 화면 밖으로 나가는지

---

## 27. 사용자 테스트를 위한 문서 (2026-08-24)

친구들에게 나가기 전에 **부모가 혼자 할 수 있어야 하는 세 가지**를 문서로 만들었습니다. 코드 변경은 없습니다.

| 문서 | 답하는 질문 |
|---|---|
| `깃허브-연결.md` | 어떻게 인터넷에 올리나 |
| `문제-만들기-안내.md` (증보) | 올린 뒤에는 문제를 어떻게 넣나 |
| `문제-만들기-프롬프트.md` (신규) | 문제를 어떻게 만드나 |

### 27.1 두 군데를 구분하지 못하면 아무것도 안 됩니다

정적 배포에서 부모가 바꾸는 것은 **두 곳으로 갈립니다.** 이걸 모르면 "부모 화면에서 문제를 추가하려다" 막힙니다.

| | 저장소 파일 | 부모 화면 |
|---|---|---|
| 무엇 | 문제·응원 메시지·소원권 | 진도·켜고 끄기·용돈·PIN |
| 어디에 저장되나 | `content/` (빌드 때 `content.json`으로 구움) | 그 브라우저의 `localStorage` |
| 언제 반영 | push 후 3~4분 | 즉시 |
| 누구에게 | **모두** | **그 아이만** |

이 갈라짐은 정적 배포의 성질이지 버그가 아닙니다. 서버가 없으니 문제 파일을 쓸 곳이 없습니다. 그래서 **문제 추가는 GitHub 웹 UI(Add file → Create new file)** 로 하도록 안내했습니다 — 부모가 git을 몰라도 브라우저만으로 됩니다.

### 27.2 문제 문장을 고치면 진도가 날아갑니다

문서에 경고로 넣었습니다. `key = f'{pack_id}:{normalize(question)}'` 이라 **문제 문장이 곧 식별자**입니다. `12 ÷ 3` → `12÷3` 만 바꿔도 새 문제가 됩니다.

- 정답·오답·힌트·묶음 칸은 마음껏 고쳐도 안전합니다
- 문제 문장은 고치지 말고 **새 줄을 추가하고 원래 줄은 부모 화면에서 끄는 것**이 안전한 길입니다

이걸 코드로 막을 수도 있었습니다(별도 id 칸). 안 했습니다 — 부모가 채워야 할 칸이 하나 늘고, 비면 어차피 문장으로 떨어집니다. 문서로 경고하는 쪽이 낫다고 판단했습니다.

### 27.3 프롬프트가 강제하는 것

`build_problems()`가 **보기를 4개로 못 만든 문제를 조용히 버립니다.** 25문제를 넣었는데 18개만 나오는 일이 실제로 생깁니다(한국사 팩을 만들 때 겪었습니다 — 24장). 그래서 프롬프트의 2번 규칙이 *오답 세 칸을 전부 채워라* 입니다. 자동 오답 생성에 기대지 않으면 이 사고가 안 납니다.

10번 규칙은 **원칙 2.1을 문제 문장까지 확장한 것**입니다. 화면의 모든 말을 다듬어놨는데 부모가 넣은 문제에 "틀리기 쉬운" 이 들어가면 거기서 깨집니다. AI가 교육용 문제를 만들 때 기본으로 넣는 표현이라 명시적으로 금지해야 합니다.

검산 단계를 프롬프트 안에 넣은 것도 같은 이유입니다. 수학 팩을 만들 때 **생성과 검산을 분리했더니 오류가 0이 됐습니다**(24장). 부모에게도 같은 방법을 줬습니다.

### 27.4 배포 전에 부모가 판단해야 하는 것

`깃허브-연결.md` 0번에 넣었습니다. 대신 결정해주지 않고 사실만 적었습니다.

- **저장소가 공개됩니다.** 무료 계정에서 Pages는 public만 됩니다. `data/`는 `.gitignore`에 있어 아이 기록은 안 올라가지만, `content/`의 문제·메시지·소원권은 올라갑니다
- **부모 화면은 잠기지 않습니다.** PIN 기본값 0000이 파일에 그대로 있습니다. 괜찮은 이유는 그 화면이 **자기 브라우저의 기록만** 건드리기 때문입니다 — 남의 아이 기록에는 닿지 않습니다
- 검토 결과 지금 `content/`에 개인 정보는 없습니다. 저장소 이름에는 아이 이름이 들어 있습니다

### 27.5 아직 안 한 것

- `설치-실행-안내.md` — README가 참조하는데 여전히 없습니다
- 부모 화면에 배포 상태(Actions 초록/빨강)를 보여주는 것. 지금은 GitHub 화면을 직접 봐야 합니다

---

## 28. 엄마의 첫 사용자 테스트 피드백 (2026-08-24)

아이가 며칠 쓴 뒤 엄마가 여덟 가지를 짚었습니다. 전부 반영했고, **셋은 짐작이 아니라 실제 결함**이었습니다.

| 피드백 | 결과 |
|---|---|
| 오답의 퀄리티가 떨어짐 | **결함이었습니다.** 199문제 중 여러 개가 계산 없이 풀렸습니다 → 0개 |
| 같은 문제가 너무 반복됨 | **결함이었습니다.** 상위 8개가 54% → 31% |
| 문제 유형이 더 다양했으면 | 심화 192문제. 문장제·거꾸로·두 단계·인과 |
| 기본과 심화로 나누면 | `난이도` 칸. 422 → **614문제** |
| 부모가 풀·별을 줄 수 있게 | 부모 화면에 「풀 · 별 주기」 |
| 소원권 조정 | `wishes.csv` 교체 |
| 잠긴 아이템도 보여주기 | 다음 단계만 회색으로 |
| 이름을 "흰양이와 공부하기"로 | 네 곳 |
| 부모 화면 정리 · 진도를 단원별로 | 섹션 9 → 7, 진도는 체크박스 |

### 28.1 오답 — 딱 떨어지는 수만 고르면 맞았습니다

```
1L는 몇 mL일까?   정답 1000   보기 996 · 1002 · 1008 · 1000
8L는 몇 mL일까?   정답 8000   보기 7992 · 7999 · 7998 · 8000
```

**계산을 안 해도 맞습니다.** `auto_wrongs()`의 `n±1, n±2` 규칙이 큰 수에서 무너진 것입니다. 3학년이 단위 변환에서 하는 실수는 1002가 아니라 **자릿수**입니다 — 100이나 10000을 씁니다.

`number_wrongs()`로 다시 썼습니다. **답의 끝자리 0 개수를 보고 오답의 자리를 거기에 맞춥니다.**

```
1000 → 10000 · 100 · 2000 · 3000      (자릿수 실수를 먼저)
 250 →  2500 ·  25 ·  260 ·  240
   4 →     5 ·   3 ·    6 ·    2      (한 자리 답은 그대로)
 0.3 →   0.4 · 0.2 ·  0.5 ·  0.1      (소수는 소수 자리를 맞춘다)
```

**규칙 한 줄: 보기 넷은 서로 같은 모양이어야 한다.** 정답만 유일하게 0으로 끝나면 그건 보기가 하나인 문제입니다.

같이 잡힌 것 — **보기가 겹치는 문제 10건**. 부모가 오답 칸에 직접 쓴 것도, 같은 묶음에서 끌어온 것도 겹칠 수 있었는데 걸러내지 않았습니다. 화면에는 같은 것이 둘 나오고 보기가 사실상 셋이 됩니다. `dedupe()`를 넣고, 걸러낸 만큼 다시 채웁니다.

### 28.2 반복 — 86문제 중 30개만 나오고 있었습니다

실제 프로필로 300문제를 뽑아 잰 값입니다.

| | 전 | 후 |
|---|---|---|
| 서로 다른 문제 | 30개 | 36개 |
| 상위 8개 비중 | **54%** | **31%** |
| 다시 나오는 최소 간격 | 4문제 | 13문제 |

원인 셋과 그 값은 기획서 11.5에 있습니다. 요약하면 `NEW_AT_ONCE` 3→5, 가중치 17.5배→3.2배, `RECENT_KEYS` 3→12입니다.

**가장 큰 손잡이는 부모가 단원을 몇 개 켰는가입니다.**

```
과목마다 1단원   서로 다른 36개 · 상위8 31%
과목마다 3단원   서로 다른 56개 · 상위8 23%
전부            서로 다른 108개 · 상위8 15%
```

`sim/repeat.mjs`를 만들었습니다. 다음에 같은 말이 나오면 짐작하지 말고 이걸 돌리세요.

### 28.3 기본과 심화 — 문제 문장은 한 글자도 안 바꿨습니다

`난이도` 칸을 CSV에 넣고 심화 192문제를 만들었습니다(수학 150 · 한국사 42).

**중요한 것은 기존 문제의 문장을 하나도 안 건드린 것입니다.** 문제 키가 `팩id:문제문장`이라 문장을 고치면 그 문제는 새 문제가 되고 아이 진도가 0으로 돌아갑니다. 그래서 seed 스크립트가 **기존 행을 그대로 다시 만들고 뒤에 심화만 덧붙이게** 했습니다. 커밋 전에 `git show HEAD:파일`과 대조해 확인했습니다.

심화를 뒤로 미는 방법도 규칙을 새로 만들지 않았습니다 — `order`에 10000을 더합니다. 한 번에 몇 개씩만 여는 기존 규칙이 그대로 적용됩니다.

### 28.4 진도 — "여기까지"를 버리고 체크박스로

```
예전:  수학 [1-4까지 ▾] [저장]        ← 한 줄로 끊는다
지금:  수학  ☑ 0 구구단  ☑ 1-3 나눗셈  ☐ 1-4 곱셈  ☑ 2-1 곱셈 …
```

부모가 원한 것은 **2학기를 먼저 열거나, 어려운 한 단원만 빼는 것**이었고 끊는 방식으로는 둘 다 안 됩니다.

`progress`가 `{ units: [...], deep: bool }`이 됐습니다. **예전 모양도 계속 읽습니다** — 지우면 이미 저장된 프로필의 진도가 사라지고 아이 화면이 갑자기 첫 단원으로 돌아갑니다. `migrateProgress()`가 부모 화면이 저장할 때 한 번 바꿔주고, *열려 있던 것은 하나도 닫지 않는다*를 테스트로 박아뒀습니다.

내장 구구단도 하나의 단원으로 껐다 켤 수 있게 했습니다(`openPacks`가 `gugudan`을 다룹니다).

### 28.5 부모 화면 — 아홉 칸에서 일곱 칸으로

| 지운 것 | 왜 |
|---|---|
| **v1 기록 가져오기** | 이관은 끝났습니다. "지금 기록을 덮어씁니다"는 남겨두기엔 위험합니다 |
| **문제 팩** (별도 칸) | 진도 목록이 같은 것을 보여줍니다. 부모가 두 곳을 오갔습니다 |
| **팩 단위 켜고 끄기** | 진도와 같은 일입니다. 둘 다 있으면 왜 안 나오는지 알 수 없습니다 |
| **PIN** (별도 칸) | 맨 위 한 줄로 |

`문제 하나씩 끄기`는 `<details>` 안으로 접었습니다. 필요하지만 자주 쓰는 것이 아닙니다.

팩 단위 끄기를 없앨 때 **예전에 꺼둔 것이 남아 있으면 이유를 알 수 없는 상태**가 됩니다. 그래서 진도를 저장할 때 `disabled.packs`를 비웁니다 — 다시 켜는 쪽이지 끄는 쪽이 아니라 원칙 2.1에 걸리지 않습니다.

### 28.6 풀 · 별 주기

프로필에 `gifts: [{at, grass, star, why, seen}]`가 생겼습니다.

- **아이 화면에는 "엄마 아빠가 풀 50개를 줬어!"만** 뜹니다. 이유는 안 말합니다 — *"심부름을 해서"* 가 화면에 뜨면 보상이 조건이 되고, 다음부터 아이는 심부름 앞에서 값을 따집니다
- **빼는 것은 한 번 더 묻습니다.** 화면 문구로도 말립니다. 벌로 쓰면 *틀려도, 안 해도 손해는 없다*가 깨집니다
- 저장은 **아이의 지금 상태를 다시 받아 얹습니다**(2.11). 아이가 놀고 있는 중에 줄 수 있습니다

### 28.7 잠긴 아이템 — 다음 단계만

5B에서 못 만난 친구의 전용 아이템을 안 보이게 한 결정(12.2)과 부딪히는 것처럼 보이지만 아닙니다. **둘은 다른 것입니다.**

| | 어떻게 |
|---|---|
| **단계로 잠긴 것** (공용) | 다음 단계 것만 회색 + "N개 더 외우면" |
| 그 다음 단계 | 안 보임 |
| **친구로 잠긴 것** (전용) | 여전히 안 보임 — 홈 화면 실루엣이 이미 하는 일 |

한 단계만 보여주면 *"조금만 더 하면 저게 열린다"* 가 되고 그건 목표입니다. 전부 보여주면 잠긴 물건 창고가 됩니다.

### 28.8 검증

```
node test/t_units.mjs   # 94개 (68 → 94)  보기 품질 · 심화 · 반복 · 단원별 진도
node test/t_parent.mjs  #  34개 (신규)     부모 화면 정리 · 풀·별 주기 · 이름
node test/t_shop.mjs    #  43개 (35 → 43)  다음 단계 미리보기
node sim/repeat.mjs     # 신규 — 반복 정도를 잰다
```

새 검사가 **실제로 결함 셋을 잡았습니다** — 보기 겹침 10건, 정답만 딱 떨어지는 문제 1건, 답이 0인 문제 1건(`500g − 300g − 200g`). 셋 다 눈으로는 못 봤을 것입니다.

가장 중요한 검사 둘:

- `"딱 떨어지는 수만 고르면 맞는" 문제 0개` — 28.1이 다시 나지 않게 합니다
- `예전 진도를 옮겨도 열린 것이 그대로` — 28.4의 마이그레이션이 아이 화면을 줄이지 않게 합니다

### 28.9 아직 안 한 것

- **단답형(6A)** — 엄마가 말한 "유형 다양화"의 진짜 답입니다. 심화는 4지선다 안에서 할 수 있는 것까지입니다
- **한국사 심화가 단원당 5~6개**뿐입니다. 수학은 계산으로 만들 수 있지만 한국사는 손으로 써야 합니다
- **`gifts` 기록이 무한히 쌓입니다.** 부모 화면은 최근 6건만 보여주지만 파일에는 다 남습니다. 몇 년 뒤에나 문제가 됩니다

---

## 29. 부모 화면이 통째로 죽어 있었습니다 (2026-08-25)

28장을 올린 다음 날 부모가 말했습니다. **"부모 화면에서 «불러오는 중…»만 계속 나옵니다."**

### 29.1 원인 — 한 줄

```js
async function showStat() { … showProgress(); … }   // 위쪽
function showProgress()  { … WARN.get(p.id) … }     // 위쪽

await showStat();                                    // 화면 그리기 시작

let WARN = new Map();                                // ← 아래쪽에 선언했다
```

`let`은 **선언 전에 읽으면 `ReferenceError`** 입니다(temporal dead zone). `var`나 함수 선언과 달리 끌어올려지지 않습니다. 28장에서 "문제 팩" 칸을 진도 안으로 합치면서 `WARN`을 그 칸이 있던 자리, 즉 부팅 호출보다 **아래**에 뒀습니다.

터진 자리가 모듈 최상위라 **그 아래 코드가 전부 실행되지 않습니다** — 팩 읽기, 소원권, 용돈, 내보내기, 되돌리기까지. 그리고 ES 모듈의 에러는 **화면 어디에도 안 뜹니다.** 부모가 보는 것은 처음 HTML에 적어둔 `불러오는 중…` 넉 줄뿐입니다.

### 29.2 왜 테스트가 못 잡았나

`t_parent.mjs`는 **글자만 훑습니다.** 「진도 저장 코드가 있는가」, 「id를 다 쓰는가」 — 34개가 전부 통과했습니다. 문법도 맞고 이름도 다 있었습니다. **틀린 것은 순서뿐**이었고, 순서는 실행해봐야 압니다.

이건 5A에서 부모 화면이 통째로 죽었던 것(2.12)과 **같은 사고입니다.** 그때는 import 이름이었고 이번엔 선언 순서입니다. 공통점은 *ES 모듈은 조용히 죽는다*입니다. 같은 사고가 두 번 났으면 검사 방식이 잘못된 것입니다.

### 29.3 한 것

**1. 모듈 최상위 상태를 한 곳으로 모았습니다.** `ALL` `WARN` `ALLOW` `dirtyProg` `DAYNAMES` — 전부 화면을 그리기 전에 선언합니다. `WEEKDAY`와 `DAYNAMES`가 같은 배열이었던 것도 하나로 합쳤습니다.

**2. 한 군데가 넘어져도 나머지는 그립니다.**

```js
await showStat().catch(boom);
showPacks().catch(boom);
```

`boom()`은 아직 `불러오는 중…`인 칸을 **"이 칸을 그리지 못했습니다"** 로 바꿉니다. 부모가 최소한 *무언가 잘못됐다*는 것은 압니다.

**3. `불러오는 중…`이 최종 상태로 남는 길을 없앴습니다.** 아이가 하나도 없으면 *"아이 화면에서 먼저 이름을 지어주세요"*, 프로필을 못 찾으면 *"되돌리기로 살릴 수 있습니다"*. 예전에는 둘 다 조용히 `return`이라 넉 줄이 그대로 남았습니다 — 정적 배포를 처음 여는 부모는 **항상** 이 화면을 봤을 것입니다.

### 29.4 `t_parentrun.mjs` — 실제로 실행해보는 검사

jsdom을 못 쓰니(외부 패키지 금지) **아주 작은 DOM을 흉내 냅니다.** `getElementById`, `innerHTML`, `value`, `dataset` 정도입니다. `<select>`에 `innerHTML`을 넣으면 첫 `option`이 골라지는 것만 특별히 흉내 냅니다 — 아이 고르기가 그것에 기대고 있습니다.

그 위에서 `parent.html`의 `<script type="module">`을 통째로 import합니다.

```
아이가 있을 때        최상위가 끝까지 도는가 · 넉 칸이 "불러오는 중"을 벗어나는가
아이가 없을 때        최상위가 끝까지 도는가 · 무엇을 해야 하는지 알려주는가
집 서버 모드          같은 것을 /api/* 흉내로 한 번 더 — 두 모드가 갈라지면 여기서 잡힌다
선언 순서             화면을 그리기 시작한 뒤에 선언되는 최상위 상태가 없는가
```

마지막 검사가 이 사고를 **다시 나기 전에** 잡습니다. 정규식으로 `await showStat()` 뒤의 `let`/`const` 선언을 찾습니다.

`dist/content.json`이 없으면 스스로 빌드합니다 — CI에서 테스트가 빌드보다 먼저 돌기 때문입니다.

### 29.5 GitHub Actions 경고

배포는 됐지만 경고 두 개가 났습니다 — *Node.js 20 is deprecated*. 액션을 올렸습니다(2026-08-25 기준 최신, 전부 `node24`).

| | 전 | 후 |
|---|---|---|
| `actions/checkout` | v4 | **v7** |
| `actions/setup-python` | v5 | **v7** |
| `actions/setup-node` | v4 | **v7** |
| `actions/upload-pages-artifact` | v3 | **v5** |
| `actions/deploy-pages` | v4 | **v5** |
| `node-version` | 20 | **22** |

경고에 `actions/upload-artifact@v4`가 끼어 있던 것은 우리가 부른 적이 없어서 이상해 보이지만, **`upload-pages-artifact`가 안에서 부릅니다.** v3이 v4(Node 20)를 불렀습니다. v5는 v7을 부릅니다.

**버전을 짐작하지 말고 확인하세요.** 없는 태그를 적으면 경고가 아니라 배포 실패입니다.

```
curl -s https://api.github.com/repos/actions/checkout/releases/latest
curl -s https://raw.githubusercontent.com/actions/checkout/v7/action.yml | grep using:
```

### 29.6 교훈

**화면을 만들면 실행해보는 검사를 같이 만드세요.** 글자를 훑는 검사는 "있어야 할 것이 있는가"만 답합니다. 이 게임에서 두 번 난 사고는 둘 다 **있는 것들의 순서**였습니다.

작은 DOM 흉내는 200줄이면 충분했습니다. 5A 때 이걸 만들었으면 사고가 한 번으로 끝났을 것입니다.

---

## 30. 초반이 쉽다 · 유형이 단조롭다 (2026-08-25)

배포 뒤 부모가 말했습니다. **"일반 문제만 초반에 풀게 되면 난이도가 쉽네요. 부모화면에서 일반만/심화만/섞어서를 고르면 좋을 듯. 문제 유형이 다양하게 나오는 방식으로."**

요청은 하나처럼 보이지만 **원인이 셋**이었고, 셋 다 잴 수 있었습니다.

| | 잰 값 | 고친 뒤 |
|---|---|---|
| 섞어 모드인데 심화가 안 나옴 | **0%** | 18% |
| 앞 문제와 같은 유형(묶음) | **45%** | 4% |
| 쉬운 구구단이 차지하는 비중 | **35%** | 20% |

### 30.1 「섞어」인데 심화가 한 문제도 안 나왔습니다

28장에서 심화를 만들 때 **출제 순서를 뒤로 미는 방식**(`order + DEEP_OFFSET`)을 썼습니다. 규칙을 새로 만들지 않으려는 선택이었고, 그 자체는 옳았습니다. 그런데 `openKeys()`가 순서 앞에서부터 `NEW_AT_ONCE`개만 열기 때문에, **그 단원의 기본을 다 지나기 전에는 심화가 한 문제도 안 열립니다.**

문서에는 *"그 단원의 기본을 지나간 뒤에 나옵니다"* 라고 적어뒀으니 명세대로 동작한 것입니다. **명세가 틀렸습니다.**

지금은 기본과 심화를 **따로 엽니다.**

```js
const lanes = b.base.length
  ? [[b.base, NEW_AT_ONCE], [b.deep, DEEP_AT_ONCE]]   // 기본 5 + 심화 2
  : [[b.deep, NEW_AT_ONCE]];                          // 심화만 고른 경우
```

`DEEP_OFFSET`은 남겨뒀습니다 — 이제 출제에는 영향이 없고, **부모 화면의 문제 목록에서 기본이 먼저 보이게** 하는 역할만 합니다.

### 30.2 난이도 세 가지

`progress.level` — `'기본' | '섞어' | '심화'`. 예전 `deep: false`는 `'기본'`으로 읽습니다. 기본값은 **`'섞어'`** 입니다.

**「심화」에서는 구구단이 통째로 빠집니다.** 구구단에는 심화가 없습니다. 안 빼면 화면의 56%가 구구단이 되어 "심화만 나오기"가 거짓말이 됩니다. 대신 부모 화면이 그렇다고 **미리 말합니다.**

이 때문에 아이 화면에서 사고가 하나 날 뻔했습니다 — 홈 화면의 `구구단 N/15`가 `P.facts[factKey(...)].m` 을 읽는데, 구구단이 색인에 없으면 **새로 시작한 아이는 그 fact 자체가 없어서** 홈 화면이 통째로 안 그려집니다. `?.m ?? 0` 으로 막고, 구구단이 안 나오는 설정이면 그 줄 자체를 숨깁니다.

### 30.3 부모 화면이 "몇 문제 나오는지"를 보여줍니다

「심화」를 고르고 단원을 하나만 켜면 낼 수 있는 문제가 **26개**입니다. 재보면 이렇습니다.

```
1단원 · 심화   서로 다른 12개 · 상위8 67% · 다시 나올 때까지 2문제 · 연달아 같은 유형 30%
전부  · 심화   서로 다른 84개 · 상위8 18% · 다시 나올 때까지 13문제 · 연달아 3%
```

**두 문제 만에 같은 것이 돌아옵니다.** 그래서 난이도 칸 바로 아래에 항상 이 숫자를 띄우고, 30개 미만이면 빨간 글씨로 *"너무 적어서 같은 문제가 자주 돌아옵니다"* 라고 말립니다. 설정을 바꾸는 즉시 숫자가 따라 바뀝니다.

### 30.4 `묶음`을 색인에 싣지도 않고 있었습니다

CSV의 `묶음` 칸은 오답을 만들 때만 쓰고 **출제에는 쓰지 않고 있었습니다.** 그런데 그 칸이 곧 **문제 유형**입니다 — `길이 바꾸기` · `나눗셈 문장제` · `거꾸로 곱셈` · `왜 그랬을까`. 부모가 이미 적어준 정보를 버리고 있었습니다.

```js
const SAME_GROUP = 0.15;   // 바로 앞 문제와 같은 묶음
const NEAR_GROUP = 0.45;   // 최근 세 문제 안에 있던 묶음
```

**빼지 않고 누르기만 합니다.** 묶음이 하나뿐인 팩에서 빼버리면 낼 것이 없어집니다. 구구단에도 묶음을 줬습니다(`구구단` / `쉬운 구구단`) — 없으면 이 규칙이 구구단에서만 안 먹습니다.

### 30.5 쉬운 구구단이 화면의 3분의 1이었습니다

2·5·9단은 `EASY`로 21문제 있고 **`gated: false`라 늘 후보에 들어갑니다.** 마스터리 2에서 시작해 가중치 4.5, 21문제면 합이 94.5 — 열려 있는 새 문제 몇 개를 압도합니다. 그리고 이것들은 **별을 안 주는 문제**입니다(`isRewardable`).

즉 아이 화면의 **셋 중 하나가 이미 아는, 보상도 없는 문제**였습니다. "초반이 쉽다"의 절반이 이것입니다.

`EASY_DAMP = 0.3`으로 눌러 20%로 낮췄습니다. **없애지 않았습니다** — 기획서 4장의 자신감용이고, 다섯에 하나쯤은 쉬운 것이 와야 합니다.

### 30.6 검증

```
node sim/repeat.mjs        # 설정 7가지를 한 표로. 심화 비중·유형 반복까지 잰다
node test/t_units.mjs      # 107개 (94 → 107)
node test/t_parentrun.mjs  #  25개 (21 → 25)
```

`sim/repeat.mjs`가 이제 **난이도별로** 잽니다. 새로 붙인 열이 `심화 비중` · `쉬운 구구단` · `연달아 같은 유형` · `최대 연속` · `한 판 유형 수`입니다.

테스트에 못 박은 것 둘:

- `섞어에서 심화가 처음부터 나온다` — 30.1이 다시 나지 않게 합니다
- `앞 문제와 같은 유형이 20% 아래` · `이어지는 길이 4 아래` — 30.4가 되돌아가지 않게 합니다

### 30.7 되짚어보면

세 가지 다 **데이터는 이미 있는데 안 쓰고 있던 것**이었습니다. `묶음`은 색인에 없었고, `deep`은 순서에만 반영됐고, `rewardable`은 별 계산에만 쓰였습니다.

새 기능을 붙이기 전에 **이미 들고 있는 값으로 무엇을 더 할 수 있는지** 먼저 보세요. 6A 단답형을 만들기 전에 이걸 한 것이 순서로도 맞았습니다.
